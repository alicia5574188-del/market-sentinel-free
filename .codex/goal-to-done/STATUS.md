# Status

Production commit `4cd0bd89144201ca91e6cbfefd31cd33543919e2` is deployed with the paired reaction shadow lab. The production page was verified directly: it scanned 127 eligible futures, created three active paired experiments, advanced radar time and candidate state across repeated observations, and resolved/opened independent continuation and reversal shadow routes. New strategy decisions are disabled (`decision=null`, `allowOpen=false`), the LIVE enable endpoint rejects activation, and the settings page reports research lock while existing position protection/reconciliation remains intact. The previous rejection audit is visible only as an archive. Verification passed 148 direct tests, 15 architecture/migration checks, TypeScript, lint, whitespace validation and production build. This deployment collects comparable forward evidence; it does not claim positive expectancy and places no new PAPER or LIVE orders.

Production commit `391ff7c7d869818a7f8a428928548e812b3c1425` deployed successfully through GitHub Actions run #609. The rejection audit V2 now publishes first-blocker, exactly-one-failed-rule, and bounded complete-combination attribution while retaining the original overlapping totals. Production verification returned ready, non-stale authority with advancing `lastSuccessAt`, no runtime error, 114 scanned futures, 660 completed/55 pending shadow samples, 3 primary-rule buckets, 1 isolated-rule bucket, 15 combination buckets and zero overflow/drops. Owner-controlled Gate LIVE remained explicitly OFF (`requestedEnabled=false`, `operational=false`). CI passed verification, build, architecture/migration checks, Cloudflare deployment and advancing production health. No strategy threshold, PAPER/LIVE order, Gate request, D1 write, risk, credential, or execution behavior changed.

Production deployment `19da59eee464bbc67b757b71f69206888d788eab` completed successfully through GitHub Actions run #608. The scored 3-of-5 anomaly admission and rejected-entry shadow audit are live. Two production reads proved ready, non-stale authority with `lastSuccessAt` advancing from 1788856865162 to 1788856910647 across 112 scanned futures. The audit immediately advanced from 10 pending/1 completed sample to 16 pending/2 completed samples with zero drops, and the production page contains the new `拦截审计` surface. Owner-controlled Gate LIVE remains explicitly OFF (`requestedEnabled=false`, `operational=false`). Local verification passed 142 direct tests, TypeScript, lint, whitespace, production build and 14 architecture/migration tests; CI verification, Cloudflare deploy and production health also passed.

The rejected-entry shadow audit is complete locally. Production data cannot retrospectively answer which filters rejected profitable candidates: it retained only current assessment state and completed real/PAPER positions, not historical rejected signals. The new research-only lane freezes one shadow trade per rejected radar event, records every failed hard gate plus each missing score component, and follows the hypothetical direction with the already-paid ten-second bulk ticker response. It mirrors the event lifecycle: stop/target reach, ten-minute no-progress exit and twenty-minute maximum hold, then records fee coverage, after-cost profitability, average net return and per-rule aggregates. The phone Review tab shows the results and waits for thirty complete samples before marking a rule as a preliminary suspected false rejection. State is bounded to 120 pending, 200 recent and 512 deduplication identities in the existing Durable Object checkpoint. It adds no Gate request, D1 write, PAPER/LIVE order or automatic rule mutation. Verification passes 142 direct tests, TypeScript, lint, whitespace, production build and 14 architecture/migration tests. No deployment or LIVE change is authorized.

The scored anomaly-admission correction is complete locally. Code comparison found the zero-trade mechanism: the first anomaly version admitted a confirmed strong event unless realtime flow was materially opposite; production now requires every NEW_MONEY/OI/flow/spread/depth/extension/cost/expectancy condition simultaneously before a separate four-book confirmation. True execution and safety constraints remain hard gates, while five directional-quality observations become a 3-of-5 score. All frozen anomaly kinds may enter the bounded realtime candidate pool; `NEW_MONEY` has explicit displacement priority, and locked exposure remains first. The runtime and operator surface expose the current score/blocker without adding Gate requests or writes. Verification passes 138 direct tests, TypeScript, lint, whitespace checks, production build, and 13 architecture/migration tests. No deployment or LIVE change is authorized.

Done on production at commit `44d79e9a5908ef176f18e5780dfa34f0a5d41ee3` through successful GitHub Actions run #606. Production follow-up first reproduced repeated `WARMING` from ten-second whole-pool churn; run #605 deployed residence hysteresis, then live inspection exposed observation-only `PRICE_SHOCK` events occupying all three slots while a seven-confirmation `WLD_USDT NEW_MONEY` remained outside. The final release makes only entry-eligible `NEW_MONEY` events resident/ranked priority, while observation-only anomalies use remaining capacity. Post-deploy observation proved `SOPH_USDT NEW_MONEY` at three confirmations and then `ARB_USDT NEW_MONEY` at six confirmations were promoted, reached 4/4 warmup with `entryReady=true`, and kept advancing `lastSuccessAt`; one bulk-radar timeout recovered on the next pass. LIVE remains user-controlled and OFF. Verification passes 138 direct tests, 13 build/architecture/migration tests, TypeScript, lint, whitespace, CI, deploy and production checks. No entry threshold, exit, sizing, cadence, UI, credential or LIVE-authority behavior changed.


Commit `9a0efd9f647774d5d54722e32b34557162dd1263` is deployed successfully through GitHub Actions run #603. The evidence-gated anomaly entry is live: production publishes frozen event kinds plus event OI/reference fields, scans 104 eligible futures, advances fresh authority data without runtime error, and leaves owner-controlled LIVE OFF. Squeeze, liquidation and unsupported price shocks are observation-only; only a qualified new-money event can arm, and it still needs four strong two-second confirmations before IOC. Verification passed 137 direct tests, 13 build/architecture/migration tests, TypeScript, lint, whitespace checks, GitHub verification, Cloudflare deploy, and advancing production health. No UI, exit, sizing-cap, cadence, credential, or LIVE-authority behavior changed.


The evidence-gated anomaly-entry correction is implemented locally and fully verified. Inspection confirmed the production defect: `eventDecision()` directly converted every retained radar anomaly into a `BREAKOUT_RETEST` plan with percentage-derived stop/target, while the candidate's NEW_MONEY/SQUEEZE/LIQUIDATION label could change during the same event. Event type is now frozen; same-direction impulses share one identity through a three-minute re-arm period; only early `NEW_MONEY` events with aligned flow, sufficient turnover, tight spread, two-sided near depth, affordable friction and positive conservative after-cost expectation may arm. A passing event uses `LOCAL_BREAKOUT` and must still complete the existing four-snapshot strong-break confirmation before IOC. A persisted same-direction close enforces five minutes of restart-safe deduplication. UI, exits, sizing caps, data cadence, and LIVE controls remain unchanged. Verification passes 137 direct tests, 13 build/architecture/migration tests, TypeScript, lint, and whitespace checks. Next: commit, push to `main`, and verify the GitHub-to-Cloudflare production release.


The chart-free operator upgrade is implemented locally and under final verification. Production inspection proved the apparent permanent 26-count was a client-only arithmetic defect: every promoted symbol had reached the authority's current 4/4 warmup threshold, but the page still subtracted from the retired value 30. The page now reads `limits.warmupSnapshots`, prioritizes open-position management text over entry readiness, and exposes the bounded all-market/three-symbol/phone-poll data contract. All browser chart code, public chart endpoints, request-time Gate 5m review fetches, D1 chart mirrors, and per-order chart events are removed. History remains complete and diagnostic but loads only on the review tab and refreshes only its newest page after initial pagination. Strategy inputs, execution, risk, and LIVE controls are unchanged. Direct verification passes 135 tests; full build/architecture/lint/deploy verification is next.

The complete PAPER audit upgrade is implemented locally and under final verification. History is cursor-paginated in deterministic newest-first order; the client retrieves every page. Brain, PAPER Orders, and owner LIVE cards no longer render charts. Historical PAPER rows use a request-time cached Gate 5m candlestick review with exact B/S execution markers and continue refreshing until twelve hours after exit. Bankruptcy logs expose their stored per-order diagnostics. No strategy, LIVE control, market scan, alarm cadence, or D1 write path changed.


All-market anomaly scalper implementation is complete locally. The Gate adapter now exposes all eligible USDT perpetual metadata plus a one-request bulk ticker radar. The authority scans every ten seconds, ranks relative price shocks against per-contract baselines, keeps up to twelve public candidates and promotes the strongest symbols into the existing three-slot two-second book/detail loop. New entries use only event decisions, current-price IOC, 0.5%–1% equity risk, a noise-aware stop and an independently economical short-term target. Passive watch symbols no longer make health depend on unused 15m/1h structure; candidates and positions still require fresh 1m evidence. The UI is renamed and reorganized around the radar, New-York-day +150 U progress, current orders/LIVE/review/settings, while prior controls and charts remain. Direct tests, build, architecture/migrations, TypeScript, lint and whitespace checks pass. Next: commit, transplant onto current `origin/main`, push main, and verify GitHub/Cloudflare production health.

Commit `1bb0691b775786e7c40f3022df7dce8f0d5204e6` deployed successfully in GitHub Actions run #595, including Cloudflare deployment and advancing-production-health verification. The UI no longer treats an optional D1 mirror warning as a trading outage; genuine stale, degraded, incomplete, or recovering market authority still fails health. Settings now exposes owner-only, double-confirmed PAPER reset and completed-history clearing. Reset requires fresh pricing for every open PAPER position, archives those closures, starts a new 1,000 U cycle, and never touches LIVE. History clearing keeps current equity, open PAPER positions and their entry reviews while deleting closed trades and account logs. Local verification passed 132 direct tests, 13 architecture/migration tests, TypeScript, lint, production build, and whitespace checks; CI and production health also passed.

Commit `0cb8ed28d0a06c1cd6405086240e456ea21a30f5` deployed successfully in GitHub Actions run #594, including Cloudflare deployment and advancing-production-health verification. RANGE/REVERSAL and BREAKOUT now share internal realtime confirmation followed by current-price IOC execution; no entry order rests on Gate. Exceptional first crosses within 0.5R can be captured, crossed stop/target geometry is rejected before plan creation, short-window OFI/microprice evidence is smoothed, and blocked routes show a concrete reason. No data cadence, D1 writes, risk limits, credentials, or LIVE control changed. Local verification passed 129 direct tests, 13 architecture/migration tests, TypeScript, lint, production build, and whitespace checks; CI and production health also passed.

# Status

Merge commit `2cdf4e4c69f24aa0392bfe834c73cec76e24f794` deployed successfully after PR #182 passed verification run #592; GitHub Actions run #593 completed the Cloudflare deployment and advancing-production-health gate. The authority now reconstructs sweep, later reclaim, and retest from the existing bounded 1m response, preserves compatible events across rolling range ids, exposes rebound and renewed-break observations together, and requires a later held retest plus first-target economics before the completed-candle rebound may execute. Structurally detected but uneconomic auctions remain visible and non-executable. Production returned ready, fresh BTC/ETH/SOL authority with advancing futures data after deployment. No request cadence, D1 writes, risk limits, credentials, or LIVE authority changed. Local and CI verification passed 127 direct tests, 13 architecture/migration tests, TypeScript, lint, production build, whitespace validation, and production health.

# Status

Merge commit `2e3294ac7b7a9b6a40bd9e67ae334c9cde9091d9` deployed successfully after PR #181 passed verification run #589; GitHub Actions run #590 completed the Cloudflare deployment and advancing-production-health gate. Production immediately reported ready, fresh BTC/ETH/SOL authority and retained the accepted BTC `BROKEN_DOWN` parent boundary as a non-chasing `LOCAL_BREAKOUT SHORT` observation at 79,550 instead of deleting all routes. Accepted parent breaks now preserve the first-segment auction, live breakout evidence can upgrade a pre-break plan without moving its geometry, ordinary breaks remain eligible for a distinct retest, failed breaks for a reverse retest, and higher-node continuation cannot enter as a standalone first leg. Every cancellation stores its exact reason/time and the page explains accepted-break observation rather than claiming no direction. No market request, alarm, D1 write, risk, credential, or LIVE-control boundary changed. Local and CI verification passed 123 direct tests, 13 architecture/migration tests, TypeScript, lint, production build, whitespace validation, and production health.

# Status

Commit `810d768ad1d8f12cd6bfb7c9aedf9814bb961de5` deployed successfully after PR #180 passed verification run #586; GitHub Actions run #587 completed the Cloudflare deployment and advancing-production-health gate. PAPER and owner-visible LIVE position cards now use browser-aggregated 5-minute close lines while retaining the existing approximately 15-second current midpoint. Entry is an exact time/price point instead of a full-width level; stop and target remain reference lines. PAPER historical reviews use the same 5-minute line convention with separate entry/exit points and stop/target context. No Gate request cadence, Durable Object loop/write, execution behavior, risk limit, credential, or LIVE state changed; LIVE remains owner-controlled and OFF. Local and CI verification passed 120 direct tests, 13 architecture/migration tests, TypeScript, lint, production build, whitespace validation, Wrangler dry-run, and production health.

# Status

Commit `456059d80ec77083276e2f89aa25ba5d4d3bab2d` deployed successfully after PR #179 passed verification run #583; GitHub Actions run #584 completed the Cloudflare deployment and advancing-production-health gate. Every open PAPER position and owner-visible Gate LIVE position now shows gross unrealized USDT, margin return, direction-adjusted price move, and the latest market price. Each card also renders a compact line chart seeded from cached Gate 1m candles and updated by the existing 15-second runtime midpoint, with entry, current protection, target, and current price labeled. No Gate feed cadence, Durable Object polling/writes, execution authority, risk limits, credentials, or LIVE state changed; LIVE remains owner-controlled and OFF. Local and CI verification passed 119 direct tests, 13 architecture/migration tests, TypeScript, lint, production build, whitespace validation, Wrangler dry-run, and production health.

Commits `84238b2bce69f226e57df9d1becda108b548f07b` and `645b1fa3f6c055890ce802dcceb7f32e0b14f603` deployed successfully after PRs #177 and #178 passed verification runs #579 and #581; GitHub Actions run #582 completed the final Cloudflare deployment and advancing-production-health gate. Production reports `liquidity-route-v6`, ready PAPER authority, current BTC/ETH/SOL data, intact page/history/candle/auth boundaries, and owner-controlled Gate LIVE remains OFF. One slow or failed Gate book now freezes only that symbol's immutable plan and blocks fills; two advancing fresh books re-arm it, while four consecutive failures or 15 seconds without fresh data cancel it. Sequence faults and structural hard invalidations remain immediate. The book freshness window is five seconds, the authority-wide stale window is eight seconds, and optional OI/trade/liquidation delays no longer stop fresh book-and-structure execution. The UI exposes frozen plans, recovery confirmation, optional-evidence degradation, total failures/recoveries, and maximum observed lag. Poll cadence, 10% aggregate risk, 6.5% same-direction risk, 30% margin cap, credentials, and request/write budgets are unchanged. Local and CI verification passed 117 direct tests, 13 architecture/migration tests, TypeScript, lint, production build, whitespace validation, and Wrangler dry-run. Initial deployment run #580 published v6 but failed only because its health script still asserted v5; PR #178 corrected all deploy/scheduled assertions and run #582 completed successfully.

Commit `5607db31d866393430147e8e5ab92f4f7d9394a3` deployed successfully after PR #176 passed verification and was squash-merged to `main`; GitHub Actions run #578 completed the production deployment and advancing-health checks. Production reports `liquidity-route-v5`, ready PAPER authority, fresh BTC/ETH/SOL evidence, zero feed failures, no runtime/D1/risk error, and Gate LIVE remains owner-controlled and OFF. RANGE execution now requires a frozen parent/child 15m sweep, reclaim by a completed 1m reversal or four strong fresh-book observations, and an economical inside retest; generic fallback RANGE catches are disabled. PAPER and LIVE share adaptive 1.5%–3% entry risk, a 10% aggregate structural-risk ceiling, a 6.5% same-direction ceiling, and the unchanged 30% margin ceiling. Pre-target dynamic protection no longer uses a 35%-of-MFE trail or crosses entry; it reduces remaining loss to 0.5R only after a completed 1m close has reached both 1.5R and 70% of the first-target path. Local and CI verification passed 116 direct tests, 13 architecture/migration tests, TypeScript, lint, production build, whitespace validation, and deployment dry-run checks. Two post-deploy reads confirmed advancing authority (`lastSuccessAt` 1788751075973 → 1788751153973).

Commit `4d375db6d941460f57f4bf691f779e8d800ef9d3` deployed successfully after PR #175 passed GitHub Actions verification run #575 and was squash-merged to `main`; main deployment run #576 completed successfully. Production now reports `liquidity-route-v4`. A boundary cross is observation, not acceptance. Direct continuation is reserved for an 86%/18% four-snapshot exceptional break; an ordinary accepted break cancels the direct lifecycle and can re-arm only after a later completed-minute retest holds the frozen boundary, followed by renewed acceleration; a completed-minute sweep-and-reclaim with absorption and reverse flow prepares a passive inside retest in the opposite direction toward the nearest same-grade liquidity. New route identities are `BREAKOUT_RETEST` and `FAILED_BREAKOUT_REVERSAL`; event-route roll-off cannot move or erase a plan while its original parent/child structure remains mapped. PAPER and LIVE share the same trigger semantics: direct and retest continuation use current-price IOC, failed-break reversal uses a passive limit. Local and CI verification passed 112 direct tests, 13 architecture/migration tests, typecheck, lint, production build, whitespace validation, and a Wrangler deployment dry-run. Two advancing production health reads confirmed live authority, fresh BTC/ETH/SOL evidence, zero feed failures, no runtime/D1/risk error, and unchanged daily budgets. LIVE remains owner-controlled and OFF; risk, margin, cadence, credentials, and existing-position protection are unchanged.

Commit `08e075c1e80136c2636649bc59661e5ee6483291` deployed successfully after PR #174 passed GitHub Actions verification run #573 and was squash-merged to `main`. The release reproduces and corrects the SOL 15m failure: completed candles create an explicit parent range plus an optional nested child; child crossings are `INTERNAL_ROTATION` toward the parent edge, while parent crossings retain `LOCAL_BREAKOUT`. Two completed closes consume an old boundary. Breakouts can be prepared only before the trigger, remember their first cross, and cancel after returning inside or exceeding 0.5R instead of reviving on a retrace. PAPER and LIVE ignore legacy farther economic targets and require the actual first target to pass economics. The UI distinguishes parent/child boundaries and hides overlays when the cached chart has not caught up to the structure candle. Local and PR verification passed 107 direct tests, 13 architecture/migration tests, typecheck, lint, production build, whitespace validation, and a Wrangler dry-run. Two post-deploy production reads confirmed `liquidity-route-v3`, PAPER mode, healthy advancing authority, fresh BTC/ETH/SOL evidence, zero feed failures, and no runtime error. Risk limits remain unchanged, existing open positions retain frozen protection, and LIVE remains owner-controlled and default OFF.

Commit `dbf284d1318862fb279c99577b170b5ed989d2c9` deployed successfully in GitHub Actions run #571 after PR #173 passed verification run #570. It fixes the zero-trade regression observed after run #568: more than seven hours of healthy production produced no new fills because all BREAKOUT plans waited for a completed minute yet still had to remain inside the 0.5R no-chase boundary, and first-node-only economics rejected a strong mapped continuation. High-quality breakouts now need three consecutive fresh two-second confirmations beyond 0.1R and inside 0.5R; medium-quality breakouts retain completed-minute confirmation. Strong mapped continuations may supply admission economics while the nearer node remains the actual first target and re-evaluation point. A just-triggered PAPER breakout is explicitly bridged into the same-cycle LIVE IOC path so the shared state transition cannot consume the real entry signal. The owner session changes from a fixed eight hours to a 30-day signed HttpOnly cookie renewed on authenticated page opening. Local verification passed 103 direct tests, 13 architecture/migration tests, typecheck, lint, production build, and whitespace validation. Two advancing production reads confirmed healthy authority, fresh BTC/ETH/SOL evidence, no runtime/D1/risk error, and a SOL plan preserving 106.83 as its current node while exposing 107.13 as its staged economic target. The deployed client bundle contains the 30-day login, three-snapshot confirmation, and real-time IOC labels; owner auth is configured but unauthenticated publicly, and LIVE remains owner-controlled and OFF.

Commit `800d3f7ccaa7e427611ab06f9fd1cc8d6fe86744` deployed successfully in GitHub Actions run #568 after PR #172 passed verification run #567. The release diagnoses the latest four closed PAPER trades rather than increasing account exposure: their combined net loss was 24.85 U, but modeled fees and stress slippage were 23.54 U (94.7%), the true directional gross loss was only 1.31 U, and none was closed by the 5% portfolio cap. The BTC BREAKOUT short was an intraminute wick through 79,526.05 whose candle closed back above the trigger. BREAKOUT is now internal observation rather than a pre-hung Gate price trigger: prepared breakouts prioritize the just-completed official Gate 1m candle for each symbol, require a directional close at least 0.1R beyond the frozen trigger with 55% range retention, and then PAPER/LIVE use the current two-second price only if it remains outside and within 0.5R; LIVE submits an IOC market order sized from that current price, while an extended move waits for an economical retest. RANGE/REVERSAL remain passive limit orders. The earlier SOL RANGE long reached about 1.2R favorable excursion and was then soft-exited around 0.6R adverse; soft exits now require three distinct completed-minute warnings plus a completed close at least 0.75R adverse, while the original structural stop remains immediate. The latest SOL position was directionally profitable by 5.48 U but net -0.31 U because its dynamic stop locked exactly the 5.78 U modeled friction; beyond-entry protection now reserves friction plus a 0.15R positive-net cushion. The ETH trade came from a frozen pre-upgrade plan with an obsolete 0.045% stop and is not evidence against the new noise floor. Risk remains 1%–1.8% per entry and 5% aggregate; wider structural stops reduce notional instead of increasing maximum account loss. Local verification passed 100 direct tests, 13 architecture/migration tests, typecheck, lint, production build, and whitespace validation. Two advancing production reads confirmed healthy authority, fresh BTC/ETH/SOL evidence, no D1 mirror error or risk breach, and owner-controlled LIVE remaining OFF.

Commits `a68f9d6938565fb54fd8af357dccb7d2d3d56c8e` and `c3cde1a57592a05d08a458d18998f34414fe47d0` deployed successfully in GitHub Actions runs #563 and #565 after PRs #170 and #171 passed verification runs #562 and #564. New plans replace sub-noise stops with the farther of structural invalidation, 0.18% modeled friction, or 1.1× the recent completed-1m range 70th percentile; require a completed 1m sweep-and-reclaim before RANGE edge execution; target the RANGE midpoint first and extend only enough for 1.2R net economics with a hard 70% cap; insert an adaptive 0.65%–0.9% 15m measured objective before a farther 1h/4h breakout node; block the same stopped route until two completed minutes and a trigger reclaim; and exit a BREAKOUT when a completed candle reaches at least 2R but retains no more than 30% of that excursion. Existing open positions retain their frozen pre-deployment protection rather than silently widening risk. PAPER and LIVE consume the same frozen plan and completed-minute evidence. Local verification passed 94 direct tests, 13 architecture/migration tests, typecheck, lint, production build, and whitespace validation. Two advancing production reads confirmed fresh healthy authority, BTC/ETH/SOL only, 0.18% or wider new-route stops, SOL RANGE targets exactly at the 15m midpoint 106.375, and owner-controlled LIVE remaining OFF.

Commit `d78a3050e13906a93d463260571a22625a62e30c` deployed successfully in GitHub Actions run #561 after PR #169 passed run #560. A valid 15m route map is now the sole entry authority: if none of its local routes is executable the market waits instead of falling back to a distant all-timeframe RANGE order. Local breakouts select the nearest 15m/1h/4h node within an adaptive 1.2%–1.5% segment, non-route activation is capped at 0.75%, and an older fallback that conflicts with an active route map is cancelled immediately. Local verification passed 90 direct tests, 13 architecture/migration tests, typecheck, lint, production build, and whitespace validation. Two advancing production reads confirmed the bad ETH no-route plan at 2,412 was `CANCELLED`, ETH decision remained WAIT, and only local routes such as 2,490.53→2,501.21 and 2,502.59→2,515.16 remained; 2,430/2,419 no longer appeared as the current first segment. Authority was healthy and fresh, BTC/ETH/SOL remained the only markets, and owner-controlled LIVE remained OFF.

Commit `5296887f42cbf65727ef9a14d109012f41f47853` deployed successfully in GitHub Actions run #559 after PR #168 passed run #558. Prepared plans now ignore transient two-second trigger-score changes and require two distinct completed 1m confirmations before route disappearance, route weakness, or activation drift can cancel them; stale data, sequence faults, expiry, crossed pre-entry invalidation, target overshoot, and failed risk/economics remain immediate hard cancellations. Open positions keep a two-minute soft-exit protection window and require the same adverse reason across three completed 1m candles plus at least 0.25R adverse price. Dynamic stops update only from completed 1m closes, remain at the original structural stop below 1R, halve remaining risk after 1R, and move beyond entry only after estimated round-trip friction plus 0.5R. The UI exposes warning progress, original/current protection levels, and labels legacy tightened `STRUCTURAL_STOP` records as `旧版即时保本止损`. Local verification passed 87 direct tests, 13 architecture/migration tests, typecheck, lint, production build, and whitespace validation. Production acceptance confirmed advancing healthy authority, working history and BTC futures candle endpoints, redacted public LIVE state, protected credentials, and owner-controlled LIVE still OFF.

Hotfix commit `f88b0fdfe716e7725151aab79be7a3da27ce8b81` deployed successfully in GitHub Actions run #557 after PR #167 passed its own verification. Route positions now make an explicit decision at the first 1h/4h liquidity node: strong executable `NODE_CONTINUATION` evidence raises the structural stop and hands the open position to the next liquidity target, while an unconfirmed continuation realizes the current segment with `TARGET_NODE_EXIT`. PAPER and LIVE positions persist their route identity, kind, target timeframe, and target score so restart and exchange reconciliation retain the same thesis. Local verification passed 82 direct tests, 13 architecture/migration tests, typecheck, lint, production build, and whitespace validation. Production acceptance reports healthy advancing `liquidity-route-v2` PAPER authority, BTC/ETH/SOL route counts of 6/6/5, a working 29-candle BTC 4h feed, and owner-controlled LIVE still OFF.

Commit `4049c24627daef45bc1bfee34cc14de3d1218bf2` deployed successfully in GitHub Actions run #554. The runtime is now `liquidity-route-v2`: each BTC/ETH/SOL card can expose both 15m boundary breakouts, edge rejections, and later node-continuation soft routes while the arbiter keeps one executable plan per symbol. The first segment ends at the nearest viable 1h/4h liquidity node; the next segment remains inactive until price reaches that node and passes the stronger continuation/fakeout check. Completed 4h charts are aggregated from the existing 1h feed without new Gate requests, and off-screen higher-timeframe targets no longer change the candle Y-axis. Dynamic PAPER/LIVE leverage targets roughly 10% margin per plan, keeps pending/open margin below 30%, preserves the 5% structural-risk cap, and retains a liquidation buffer. Local verification passed 80 direct tests, 13 architecture/migration tests, typecheck, lint, production build, and whitespace validation. Production acceptance showed advancing LIVE-state market data, 6 BTC routes, 6 ETH routes, 5 SOL routes, a 40× BTC PAPER plan using 88.93 U estimated margin on 889.33 U equity, and a working BTC 4h endpoint with 29 completed candles. Existing PAPER equity was preserved and owner-controlled LIVE remained OFF.

Commit `7f4b04502f07ebf16b7e79455a68fac7a154ad9c` deployed successfully in GitHub Actions run #552. It fixes the 10 U LIVE enable failure shown as `SOL_USDT 可用保证金不足一个合约`. LIVE still scales PAPER sizing by actual account equity; when that result is below Gate's indivisible one-contract lot, it now uses one lot only if the lot's real margin, net economics, and aggregate stop loss remain within the 5% account boundary. A capacity-limited BTC/ETH/SOL plan becomes a visible `本轮未挂单` item and cannot disable LIVE or block other affordable plans. Actual Gate/API/submission conflicts remain fatal and retain full rollback. Local verification passed 76 direct tests, 13 architecture/migration tests, typecheck, lint, production build, and whitespace validation; the workflow also passed deployment and advancing production-health acceptance.

Commit `ad6e050188e10295eb547d94bb4175996be4b576` deployed successfully in GitHub Actions run #551. Shared PAPER/LIVE sizing now caps per-entry risk at 1%–1.8% and notional at 4× account equity, preserves the 5% portfolio ceiling, and rejects targets below 1.5% of equity after modeled costs. PAPER automatically archives a detailed bankruptcy report at 300 U and starts a new 1,000 U cycle only after remaining PAPER positions are closed on fresh prices. History has a dedicated Account Logs view with current-cycle state, permanent bankruptcy cards, root-cause metrics, breakdowns, and copyable full JSON. Local typecheck, lint, all 74 direct tests, 13 architecture/migration tests, production build, whitespace validation, deploy, and advancing production monitoring pass. Production acceptance also verified the new account-log endpoint returns an array; the deployment preserved existing checkpoint equity and live-mode state and did not mutate Gate credentials.

Commit `0b4e73f991b2afc55db665f1753df6751b4ba188` deployed successfully in GitHub Actions run #550. Production history contained 7 losses totaling -110.67 U; every position lasted exactly two seconds, while modeled round-trip costs totaled 107.12 U and price movement contributed only -3.55 U. Two orders closed at the identical entry price and one had positive gross movement but negative net PnL after costs. The release blocks entries below 1.2 net R after costs, reconfirms absorption for RANGE/REVERSAL triggers, requires target continuity at crossing, and confirms non-stop liquidity exits with two distinct completed 1m candles. Structural stops remain immediate. History now lazily renders bounded actual Gate 1m candles with exact entry/exit markers and a gross/cost/net breakdown. Local typecheck, lint, 69 direct tests, 12 architecture/migration tests, production build, and whitespace validation pass. Production health advances every two seconds with no D1 mirror error; all 7 existing orders return ready actual Gate 1m reviews containing 30–41 candles, and LIVE remained OFF throughout deployment.

Done on production at commit `7fc1c425a8ffffc66938937b45083feb7e9c3d1e`. GitHub Actions run #538 completed verify, deploy, and monitor successfully, including advancing PAPER authority, schema/credential-presence checks, homepage/history smoke tests, and the actual Gate futures candle proof.

The authoritative universe is permanently BTC, ETH, and SOL; checkpoint recovery removes ZEC and every prior rotating symbol. Each market has an independent freshness decision, so one recovering feed no longer labels every coin incomplete.

Follow-up deployed at commit `2bd3200ddcc5a604656f06e2d9b3632d3700398d`; GitHub Actions run #539 completed successfully. The runtime API was healthy and all three markets reported both primary and ancillary evidence fresh. The false `数据恢复中` display was traced to the browser treating a single request lasting over five seconds as a total market failure. The UI now allows a 30-second request and retains a verified runtime snapshot for 90 seconds across transient transport delay; backend per-market freshness remains authoritative and still blocks stale-price entry. Production browser verification now shows `后台运行中`, BTC and SOL `继续观察`, ETH `等待进场`, live prices, and all three 72-candle charts.

Second follow-up deployed at commit `79f9bdaff22ace37622fad8d78f8c8ca228deb10`; GitHub Actions run #540 completed successfully. Transport freshness is now presentation-only: a slow phone refresh shows a page-delay notice while preserving the last authoritative per-market state, and only actual backend `stale`, `fresh`, or `ancillaryFresh` failures produce `数据恢复中`. Order cards now identify `notional` as PAPER futures contract notional and show a display leverage tier plus estimated margin, with an explicit note that trigger-time price/equity/risk are recalculated and no Gate order is submitted. Production verification showed backend running, BTC observing, ETH and SOL waiting to trigger, all three candle charts, and order-card values including 10×/391.49 U and 20×/534.62 U.

Commit `76070bd8fe74f2117d914670a2ffa83fe0d9287b` deployed successfully in run #541 in 2m38s, down from #540's 3m12s. It fixes the zero-fill plan lifecycle found from production evidence: the former 90-second plan expired before distant predictive entries could be reached, neutral or slightly drifting two-second decisions cancelled frozen intent, and sub-friction targets appeared in Orders only to fail trigger-time economics. Plans now freeze for 15 minutes, survive neutral/same-side drift, and pass fee/slippage economics before display. A neutral-tick crossing is directly tested to open PAPER. Commit `683e70c7d5d6528dde83c02bc4d5450f8d0bb460` then deployed successfully in run #543, making all card levels use the frozen plan and serializing scheduled monitoring with deployments. Production acceptance after warmup showed `后台运行中`, three executable positive-R:R plans, fixed bottom navigation, tab selection at scrollY 0, consistent card/execution/chart levels, and all three plan levels unchanged across a further 35-second live observation. Local typecheck, lint, 53 direct tests, 12 architecture/migration tests, build, and diff check pass.

Each card displays actual completed Gate USDT futures candles with working 1m, 15m, and 1h switches. MarketStream mirrors its own nine strategy candle sets to the existing D1 settings row at most once per five minutes, keeping chart reads independent from the two-second trading loop. The chart contains candles, liquidity regions, and strategy/position levels; adjacent Chinese text states direction and whether the system is waiting for real-time price, forming a plan, or holding. A failed period switch can never relabel an older period.

Production visual acceptance observed `后台运行中`, exactly three market cards, 216 rendered candles, no ZEC, and distinct BTC 1m versus 1h OHLC values. The release remains physically PAPER-only; the public LIVE control is visible but security-locked and no Gate mutation endpoint exists.

Commit `cba470f233121875b6ccb382dcf772c6e5314345` deployed successfully in GitHub Actions run #544 in 2m25s. It fixes the remaining direction-flip race for the shared BTC/ETH/SOL state machine: an opposite two-second recalculation can no longer cancel or replace a PREPARED plan, and a fresh trigger crossing is preserved through the runtime's planning pass so the execution pass opens the original frozen side. Two SOL-shaped regression tests cover both approach and exact crossing. Local direct tests pass 55/55, architecture/migration tests pass 12/12, and build, typecheck, lint, and diff check pass. Production reports `后台运行中`; BTC and ETH kept identical frozen side/trigger/stop/target levels across another 25-second observation while live prices and liquidity evidence continued updating.

The current release candidate adds the owner-controlled Gate live layer without forking the strategy. The owner logs in as `owner` using the existing backend access code, receives an eight-hour signed HttpOnly session, and alone can turn live execution on or off. Live starts OFF after the upgrade. Enabling it reuses the frozen BTC/ETH/SOL plan levels and dynamic position management: BREAKOUT uses a Gate price-trigger market entry, REVERSAL/RANGE use GTC limit entries, and every fill receives a close-only structural stop. Disabling cancels unfilled entries while existing positions retain exchange protection and strategy exits. Pending entries and open positions share the same 5% account risk cap, same-cycle margin is reserved, ambiguous submissions are reconciled before any retry, and unknown Gate positions/orders or dual-side mode fail closed. The existing encrypted live credential row remains untouched. Local typecheck, lint, 62 direct tests, 12 architecture/migration tests, production build, and whitespace validation pass.

Commit `5e47df241266e71cf0e43715aab0c7a2dd6b65b9` deployed successfully in GitHub Actions run #546. It fixes the real-order rollback defect observed after a failed enable. All BTC/ETH/SOL live intents are now sized and checked as one batch before the first Gate mutation. If submission later fails, LIVE is forced OFF and every system-tagged partial entry is reconciled and cancelled. Turning LIVE off always performs the same exchange reconciliation, even when runtime memory contains no pending entries; an authenticated manual cleanup button exposes this recovery without requiring another enable attempt, and never touches untagged manual orders. The dashboard now has separate Live Account, Live Orders, and API Management views. Owner API save/replace validates Gate read-only and encrypts server-side without enabling LIVE; deletion checks that LIVE is off and Gate is flat with no orders. The scheduled monitor accepts the intentional zero-credential state and still verifies the credential API is unauthorized publicly. Local typecheck, lint, 64 direct tests, 12 architecture/migration tests, production build, and whitespace validation pass. Production acceptance verified advancing health, the homepage/history/candle feeds, public runtime redaction, owner auth configuration, and unauthenticated rejection of the credential endpoint.

Commit `07b5b01293beb6f1c44d307961639f1dd506f61c` deployed successfully in GitHub Actions run #547. The decision hero, PAPER equity/PnL/position/risk cards, and PAPER transport notices now belong only to the Brain tab. Orders, Live, History, and Settings begin directly with their own content while the iPhone navigation remains fixed at the bottom.

Commit `fa6ab15d9048fef576c9a5c7ecdfa6eda42ab60c` deployed successfully in GitHub Actions run #548. It fixes the Gate cleanup failure found on the live account page. Gate int64 order IDs are preserved as exact strings instead of passing through lossy JavaScript numbers. Both closing LIVE and the authenticated stale-order cleanup now cancel system entry orders from a fresh exchange snapshot, reread Gate after cancellation, retry once if necessary, and fail visibly while keeping LIVE off if Gate still reports an order. Manual orders and protective stops remain untouched. Local typecheck, lint, 66 direct tests, 12 architecture/migration tests, production build, and whitespace validation pass. Production health is ready with advancing authority and BTC/ETH/SOL only.

Commit `8528f11965fe3ae17d404f2cb636f63e198c2160` deployed successfully in GitHub Actions run #549. It fixes the Gate trigger-expiration rejection by using the exchange-valid one-day expiry for entry triggers and 30-day expiry for close-only protective stops; the strategy's shorter internal plan lifetime still cancels stale entries. It preserves every main tab's scroll position and every Live subview's independent scroll and selection for the current browser session while keeping panels mounted, removes the redundant top-right PAPER/LIVE switch, and keeps the fixed bottom navigation as the only global navigation. The PAPER Orders page now separates current positions, waiting entries, and trades closed in the last 15 minutes; history refreshes every 15 seconds so a position opened and closed between runtime polls remains visible. Live-mode 409 responses immediately synchronize the actual backend switch state, Gate trigger errors are translated for the owner, and the mobile health label is no longer hidden. Local typecheck, lint, 66 direct tests, 12 architecture/migration tests, production build, and whitespace validation pass. Production health is ready, authoritative state advances every two seconds, all BTC/ETH/SOL evidence is fresh, and LIVE remained OFF throughout deployment.
