# Status

Rebuild is implemented on rebuild/three-state-core. Run #513 passed Gate zero-inventory, credential, D1 prepare, and transitional v6 deployment, but stopped before v7. The sanitized snapshot proved all four valid Gate books were rejected because the two-second idempotency slot floor—not the actual invocation time—was used as their freshness clock; every symbol stayed at warmup zero. The runtime now keeps the slot only for replay protection and validates exchange timestamps against actual time. The temporary token is removed; legacy DO deletion and D1 purge have not run.

MarketStream storage is the PAPER authority. D1 is an idempotent versioned mirror with an outbox. The release workflow uses an immutable trusted read-only Gate preflight, complete 15-column credential fingerprinting, v6 create-only deployment and health/restart checks, v7 legacy Durable Object retirement, then the explicit D1 purge. Direct behavior tests, migration tests, clean build, typecheck, lint, and artifact scans pass locally; no production action was taken.
