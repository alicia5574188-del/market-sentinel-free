name: Verify Timely Source Protection
on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/timely-protection-stage.yml'
      - 'scripts/timely-protection-apply.py'
      - 'scripts/timely-protection-transfer-*.b64'
permissions:
  contents: write
jobs:
  stage:
    if: github.head_ref == 'fix/timely-protection' && github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22.16.0'
      - name: Capture exact old production metadata, no login or private calls
        run: |
          mkdir -p /tmp/timely-protection-verified
          curl -fsS --retry 2 --max-time 20 'https://market-sentinel-free.alicia5574188.workers.dev/__health' > /tmp/timely-protection-verified/health-before.json
          date -u +'%Y-%m-%dT%H:%M:%SZ' > /tmp/timely-protection-verified/time-before.txt
      - name: Apply exact checked changes and remove temporary transport
        run: |
          cp scripts/timely-protection-transfer-*.b64 /tmp/timely-protection-verified/
          python scripts/timely-protection-apply.py
          git rm --ignore-unmatch scripts/timely-protection-transfer-1.b64 scripts/timely-protection-transfer-2.b64 scripts/timely-protection-apply.py .github/workflows/timely-protection-stage.yml
      - name: Verify unchanged discovery, timely source exits, frozen old positions and both LIVE adapters
        shell: bash
        run: |
          set -euo pipefail
          npm ci 2>&1 | tee /tmp/timely-protection-verified/install.log
          npm run test:direct 2>&1 | tee /tmp/timely-protection-verified/direct.log
          npm run test:forward 2>&1 | tee /tmp/timely-protection-verified/forward.log
          npm test 2>&1 | tee /tmp/timely-protection-verified/build-test.log
          npm run typecheck 2>&1 | tee /tmp/timely-protection-verified/typecheck.log
          npm run lint 2>&1 | tee /tmp/timely-protection-verified/lint.log
          npx wrangler deploy --dry-run --config dist/server/wrangler.json 2>&1 | tee /tmp/timely-protection-verified/dry-run.log
          git diff --check
          git add -A
          git diff --cached --check
      - name: Store verified blobs only; connector controls branch and reviewed release
        uses: actions/github-script@v7
        with:
          script: |
            const fs=require('fs'),cp=require('child_process'),crypto=require('crypto');
            const {owner,repo}=context.repo,parent=context.payload.pull_request.head.sha,branch=context.payload.pull_request.head.ref;
            const current=await github.rest.repos.getBranch({owner,repo,branch});
            if(current.data.commit.sha!==parent)throw new Error('Head changed during verification');
            const base=await github.rest.git.getCommit({owner,repo,commit_sha:parent});
            const names=cp.execFileSync('git',['diff','--cached','--name-only','-z']).toString().split('\0').filter(Boolean),tree=[],sha256={};
            for(const path of names){
              if(!fs.existsSync(path)){tree.push({path,mode:'100644',type:'blob',sha:null});continue;}
              const bytes=fs.readFileSync(path),blob=await github.rest.git.createBlob({owner,repo,content:bytes.toString('base64'),encoding:'base64'});
              const expected=crypto.createHash('sha1').update(Buffer.concat([Buffer.from('blob '+bytes.length+'\0'),bytes])).digest('hex');
              if(blob.data.sha!==expected)throw new Error('Blob mismatch: '+path);
              tree.push({path,mode:'100644',type:'blob',sha:blob.data.sha});sha256[path]=crypto.createHash('sha256').update(bytes).digest('hex');
            }
            const result={parent,baseTree:base.data.tree.sha,tree,sha256,runId:context.runId,branchRefUnchanged:true,productionUnchanged:true};
            fs.writeFileSync('/tmp/timely-protection-verified/verified-tree.json',JSON.stringify(result,null,2));console.log(JSON.stringify(result));
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: timely-protection-verified-${{ github.run_id }}
          path: /tmp/timely-protection-verified/
          retention-days: 7
          if-no-files-found: error
