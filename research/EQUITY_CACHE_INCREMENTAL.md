# Retain equity history and fetch only new archive entries

## Persistent reopen correction — 2026-09-19
The owner explicitly requires closing/reopening the application, not just switching tabs. The earlier sessionStorage implementation did not meet that lifetime. `incremental-persistent-v1` now uses same-origin localStorage with the unchanged authenticated owner/member + original PAPER account + initial-capital namespace. Synchronously restore all saved raw points and both cursors before the chart loader makes a request. Valid legacy sessionStorage is migrated without a network read and removed only after the persistent write succeeds. Schema accepts the preceding cache version; a UI release never intentionally expires history.

Login expiry/logout hides the Dashboard and cancels its outstanding work but no longer deletes the saved PAPER projection. An unauthenticated startup must not hydrate or display it; successful authentication under the same identity may restore it. Other users/new PAPER accounts do not inherit it. Persist only observed PAPER net values, timestamps, policy labels and read cursors; never login keys, sessions, Gate credentials, private LIVE amounts or automatic-switch state. A browser profile holder can inspect its local website data; this is not encryption or an offline login bypass.

Remove the silent 20,000-point persistence cutoff. Writes remain subject to the browser's native quota, with an 8-million-character read/serialization safety bound. Failure keeps the last successful checkpoint/cursors and current memory, shows a brief notice, and resumes from the last checkpoint rather than automatically purging all history. No timer/TTL, deployment, ordinary restart or renewed login clears the cache. User-cleared site data, private browsing, device/profile changes, browser eviction and storage denial remain exceptions; no web app can promise to survive deletion of its storage.

No backend file, deployment workflow, Cloudflare resource cap, server write/alarm/Gate request, strategy, source or execution cadence is changed. New records continue through the same bounded authenticated `after` route. First-time or genuinely unread history is still paged. Tests add close/reopen with separate persistent/session stores, schema migration, migration failure, auth renewal, member/account isolation, corrupted cache fallback, last-good-quota checkpoint recovery and >20,000 points. A real fresh-browser-process regression must run on an available authorized CI browser, not be described as passed from component remounts. Local browser navigation is administratively blocked; do not bypass it.

This section supersedes session-only lifetime and automatic cache deletion below. The preceding implementation is retained here as release history, not the current behavior.

## Request and confirmed cause
The owner reports that switching tabs destroys the equity curve and restarts historical loading. The Dashboard conditionally mounts the chart. Previously its history points and exclusive older cursor lived in component state; unmount discarded both. Each remount/range-effect restarted page-one fetch, even though the underlying archive is immutable. This was not a Gate market fetch or a new trade, but repeated Worker/DO requests and archive reads are not free of resource use. Do not infer actual quota exhaustion or a billed amount from the screenshot.

## Minimal display-only correction
- One cache instance belongs to the authenticated Dashboard, above the conditionally mounted chart. Already read points and older-page progress survive tab/range changes. Only the current GET may finish after unmount; no further history requests while hidden. Logout/account identity replacement aborts and invalidates pending work.
- A bounded, optional sessionStorage copy persists verified projection tuples and both cursors within the same browser tab across reload. Namespace is authenticated owner/member ID plus original PAPER account identity/initial capital, not a key or credential. Logout clears this cache prefix. New source accounts cannot inherit old points. Storage denial/quota/corruption falls back without deleting any in-memory or server financial records. At most20,000points/2million serialized characters are persisted; larger histories remain in memory and on server, not silently truncated. Closing the browser tab, clearing its data, switching devices or unavailable local storage may require a new initial read.
- Existing authenticated GET /api/forward/equity gains `after=<exact archive key>`. Initial/older pages retain descending exclusive cursor semantics; new pages use documented ascending exclusive startAfter with65raw-entry bound/64returned. Key-based watermark advances even across event-only/split/non-equity packets. Reconnection catches up all intervening new pages, never falsely declares an incomplete delta fresh. Past points keep their recorded amounts; conflicting overlap stops projection updates and invalidates advice.
- The already-fetched runtime's lastCycleAt suppresses latest checks when the source has no new completed cycle. New checks are at most once/minute while this chart is visible. Long first load/offline catchup remains paged in batches of32 with700ms spacing/30second yields. Reader's existing shared cache/singleflight/rate caps retained. Delta pages expire like latest pages, never cached indefinitely like old immutable pages. Changing displayed range does not discard any loaded data.
- In-memory/shared state is instance-local, never a global cross-request SSR cache. Guests still cannot load program data. No automatic LIVE authority or new private-account API added. Historical marks/smoothing/7day advice conditions unchanged. A runtime snapshot older than retained history must not erase newer confirmed marks from the drawing. Advisory stays unavailable on cache errors/incomplete catchup.

## Boundaries and verification
No changes to source discovery, exits, risk, size/leverage, scan/refit cadence, forward storage writer, Gate calls, alarms, member execution, credentials, real owner switch or immutable financial records. Only the optional read endpoint receives one new argument. Existing frozen source and all critical primary-method tests remain unaltered.

New tests cover cached tab reentry/reload/partial-load resume, exclusive delta catchup, event-only pages, concurrent calls, hidden charts, account namespaces, malformed/denied browser storage, logout and late responses, auth failure, rate backoff and expiring negative incremental reads. Actual Dashboard/EquityCurve tested offline at320/393/768/1440 with synthetic quotes and an explicitly injected sessionStorage stand-in:150marks/3initialpages;5round-trip tab switches add0requests; full App remount adds0requests;1new cycle adds1after-only request. Curve paths must contain all149connections and remain equal over unchanged remounts; no JS errors/overflow. This is not a real-login, physical-iPhone, real-browser-storage or production-account test. Container browser navigation was administratively blocked; no settings were bypassed, so network UI navigation is not claimed. SessionStorage serialization/hydration is independently unit-tested.

Cloudflare pricing depends on the configured backend and plan; get/list still has read accounting and Worker/DO requests/compute remain. The reduction is eliminating duplicate history work, not claiming zero resource cost, higher quotas or an account-wide quota cure. Official documentation checked: https://developers.cloudflare.com/durable-objects/platform/pricing/ and https://developers.cloudflare.com/durable-objects/api/legacy-kv-storage-api/#supported-options-2 . React component unmount state semantics: https://react.dev/learn/preserving-and-resetting-state .

Ship only after exact-source independent tests, normal reviewed-main CI and an actual advancing/public/source/owner-continuity receipt. No personal login, switches or real trading used for release verification.