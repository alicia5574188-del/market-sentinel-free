import { writeFileSync } from 'node:fs';

const SYMBOLS=['BTC_USDT','ETH_USDT','SOL_USDT','XRP_USDT','BNB_USDT','DOGE_USDT','ADA_USDT','LINK_USDT','LTC_USDT','AVAX_USDT','BCH_USDT'];
const STEP=300,DAY=86400,WINDOW=120*DAY,CHUNK=3*DAY,ROLL=7*24*12;
const BASE_COST=.00165,STRESS_COST=.00270,FRAC=.10,ENTRY_BUDGET=2.5,MAX_EXPOSURE=1.5,MAX_PER_STEP=3;
const HOLDS=[2,4,8,12],QS=[.50,.70,.85];
const END=Math.floor(Date.now()/1000/STEP)*STEP,START=END-WINDOW,S1=START+30*DAY,S2=S1+30*DAY,S3=S2+30*DAY;
const split=t=>t<S1?'discovery':t<S2?'validation':t<S3?'evaluation1':'evaluation2';
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
async function get(url,tries=7){for(let a=0;a<tries;a++){try{const r=await fetch(url,{headers:{Accept:'application/json'}});if(r.ok)return await r.json();if(r.status!==429&&r.status<500)throw new Error(`${r.status} ${url}`);}catch(e){if(a===tries-1)throw e;}await sleep(300*(a+1));}}
const n=x=>{const v=Number(x);return Number.isFinite(v)?v:null;};
function norm(x){const time=n(x.time),mark=n(x.mark_price),oi=n(x.open_interest_usd??x.open_interest);if(!(time>0)||!(mark>0)||!(oi>0))return null;return {time,mark,oi,takerLong:Math.max(0,n(x.long_taker_size)??0),takerShort:Math.max(0,n(x.short_taker_size)??0),longLiq:Math.max(0,n(x.long_liq_usd_new??x.long_liq_usd)??0),shortLiq:Math.max(0,n(x.short_liq_usd_new??x.short_liq_usd)??0),acct:n(x.lsr_account),top:n(x.top_lsr_size),funding:n(x.last_funding_rate)??0,longUsers:Math.max(0,n(x.long_users)??0),shortUsers:Math.max(0,n(x.short_users)??0)};}
async function fetchSymbol(symbol){const out=[];for(let from=START-ROLL*STEP;from<END;from+=CHUNK){const to=Math.min(END-1,from+CHUNK-1),url=`https://api.gateio.ws/api/v4/futures/usdt/contract_stats?contract=${encodeURIComponent(symbol)}&from=${from}&to=${to}&interval=5m&limit=1000`;const a=await get(url);if(Array.isArray(a))out.push(...a.map(norm).filter(Boolean));}return [...new Map(out.map(r=>[r.time,r])).values()].filter(r=>r.time>=START-ROLL*STEP&&r.time<END).sort((a,b)=>a.time-b.time);}
const raw=new Map();let cursor=0;async function worker(){while(cursor<SYMBOLS.length){const s=SYMBOLS[cursor++];try{raw.set(s,await fetchSymbol(s));}catch(e){console.error('FETCH_FAIL',s,String(e));raw.set(s,[]);}}}await Promise.all(Array.from({length:6},worker));

class RollZ{constructor(max){this.max=max;this.q=[];this.head=0;this.sum=0;this.sq=0;}push(v){if(!Number.isFinite(v))return;this.q.push(v);this.sum+=v;this.sq+=v*v;while(this.q.length-this.head>this.max){const x=this.q[this.head++];this.sum-=x;this.sq-=x*x;}if(this.head>4096&&this.head*2>this.q.length){this.q=this.q.slice(this.head);this.head=0;}}z(v){const k=this.q.length-this.head;if(!Number.isFinite(v)||k<288)return null;const m=this.sum/k,vr=Math.max(1e-12,this.sq/k-m*m);return (v-m)/Math.sqrt(vr);}}
const clamp=(x,a,b)=>Math.max(a,Math.min(b,x)),sgn=x=>x>0?1:x<0?-1:0;
const events=[];
for(const [symbol,rows] of raw){if(rows.length<ROLL+100)continue;const idx=new Map(rows.map((r,i)=>[r.time,i])),rz={r5:new RollZ(ROLL),r15:new RollZ(ROLL),oi15:new RollZ(ROLL),oi60:new RollZ(ROLL),taker:new RollZ(ROLL),acct:new RollZ(ROLL),top:new RollZ(ROLL),div:new RollZ(ROLL),user:new RollZ(ROLL),liq:new RollZ(ROLL),fund:new RollZ(ROLL)};
  for(let i=0;i<rows.length;i++){const r=rows[i],p1=idx.get(r.time-STEP),p3=idx.get(r.time-3*STEP),p12=idx.get(r.time-12*STEP),a1=p1===undefined?null:rows[p1],a3=p3===undefined?null:rows[p3],a12=p12===undefined?null:rows[p12];const r5=a1?Math.log(r.mark/a1.mark):null,r15=a3?Math.log(r.mark/a3.mark):null,oi15=a3?Math.log(r.oi/a3.oi):null,oi60=a12?Math.log(r.oi/a12.oi):null,taker=Math.log((r.takerLong+1)/(r.takerShort+1)),acct=r.acct>0?Math.log(r.acct):null,top=r.top>0?Math.log(r.top):null,div=Number.isFinite(acct)&&Number.isFinite(top)?acct-top:null,user=Math.log((r.longUsers+1)/(r.shortUsers+1)),liqTot=r.longLiq+r.shortLiq,liq=Math.log1p((liqTot/Math.max(r.oi,1))*1e6),liqSkew=(r.shortLiq-r.longLiq)/(liqTot+1),fund=r.funding;
    const z={r5:rz.r5.z(r5),r15:rz.r15.z(r15),oi15:rz.oi15.z(oi15),oi60:rz.oi60.z(oi60),taker:rz.taker.z(taker),acct:rz.acct.z(acct),top:rz.top.z(top),div:rz.div.z(div),user:rz.user.z(user),liq:rz.liq.z(liq),fund:rz.fund.z(fund)};
    if(r.time>=START&&Object.values(z).every(Number.isFinite)){
      const dP=sgn(z.r15),dT=sgn(z.taker),dL=sgn(liqSkew),dA=sgn(z.acct),dD=sgn(z.div);
      const add=(family,dir,score)=>{if(dir&&Number.isFinite(score)&&score>0)events.push({symbol,t:r.time,family,dir,score,zR5:z.r5,zR15:z.r15,zOi15:z.oi15,zOi60:z.oi60,zTaker:z.taker,zLiq:z.liq,liqSkew,zAcct:z.acct,zDiv:z.div,zUser:z.user,zFund:z.fund,split:split(r.time)});};
      if(Math.abs(z.r15)>=1&&dT===dP&&z.oi60>=.25)add('FRESH_BREAKOUT',dP,Math.abs(z.r15)+.55*Math.abs(z.taker)+.45*Math.max(0,z.oi60));
      if(Math.abs(z.r15)>=1&&z.oi15<=-.5&&z.liq>=1)add('DELEVERAGE_REV',-dP,Math.abs(z.r15)+.65*z.liq+.45*Math.max(0,-z.oi15)+.25*Math.abs(liqSkew));
      if(Math.abs(z.r15)>=1&&z.oi15<=-.5&&z.liq>=1)add('DELEVERAGE_CONT',dP,Math.abs(z.r15)+.65*z.liq+.45*Math.max(0,-z.oi15)+.25*Math.abs(liqSkew));
      if(z.liq>=1.25&&Math.abs(liqSkew)>=.35){add('LIQ_REV',-dL,z.liq+.8*Math.abs(liqSkew)+.3*Math.abs(z.r5));add('LIQ_CONT',dL,z.liq+.8*Math.abs(liqSkew)+.3*Math.abs(z.r5));}
      if(Math.abs(z.taker)>=1.5&&Math.abs(z.r5)<=.8)add('ABSORPTION',-dT,Math.abs(z.taker)+(.8-Math.abs(z.r5))+.25*Math.max(0,z.oi15));
      if(Math.abs(z.taker)>=1.25&&z.oi15>=.25)add('TAKER_MOM',dT,Math.abs(z.taker)+.45*Math.max(0,z.oi15)+.25*Math.abs(z.r5));
      if(Math.abs(z.div)>=1.25&&Math.abs(z.oi60)>=.5)add('TOP_FOLLOW',-dD,Math.abs(z.div)+.35*Math.abs(z.oi60)+.2*Math.abs(z.taker));
      if(Math.abs(z.acct)>=1.25&&Math.abs(z.r15)>=.75&&dA===dP)add('CROWD_FADE',-dA,Math.abs(z.acct)+.4*Math.abs(z.r15)+.25*Math.abs(z.div));
      if(Math.abs(z.r15)>=1&&z.oi60>=.75&&dT===dP)add('OI_BREAK',dP,Math.abs(z.r15)+.65*z.oi60+.35*Math.abs(z.taker));
      if(Math.abs(z.r15)>=1&&z.oi60>=.75&&dT===-dP)add('OI_FAIL',-dP,Math.abs(z.r15)+.65*z.oi60+.35*Math.abs(z.taker));
      if(Math.abs(z.fund)>=1.5&&Math.abs(z.acct)>=1&&sgn(z.fund)===dA)add('FUND_CROWD',-dA,Math.abs(z.fund)+.5*Math.abs(z.acct)+.2*Math.abs(z.r15));
    }
    for(const [k,v] of Object.entries({r5,r15,oi15,oi60,taker,acct,top,div,user,liq,fund}))if(Number.isFinite(v))rz[k].push(v);
  }
}
// family/symbol cooldown to prevent one shock from generating many near-identical entries
const last=new Map(),dedup=[];events.sort((a,b)=>a.t-b.t||a.family.localeCompare(b.family)||a.symbol.localeCompare(b.symbol));for(const e of events){const k=`${e.family}|${e.symbol}`,p=last.get(k)??-Infinity;if(e.t-p<15*60)continue;last.set(k,e.t);dedup.push(e);}
function trade(e,hold,orientation,cost){const rows=raw.get(e.symbol),map=new Map(rows.map(r=>[r.time,r])),en=map.get(e.t+STEP),ex=map.get(e.t+(hold+1)*STEP);if(!en||!ex)return null;const gross=orientation*e.dir*(ex.mark/en.mark-1);return {...e,hold,orientation,entryTime:en.time,exitTime:ex.time,gross,net:gross-cost};}
const qtile=(a,q)=>{if(!a.length)return Infinity;const b=[...a].sort((x,y)=>x-y),p=(b.length-1)*q,i=Math.floor(p),f=p-i;return b[i]+(b[Math.min(i+1,b.length-1)]-b[i])*f;};
function metrics(a){if(!a.length)return {trades:0};const v=a.map(x=>x.net),sum=v.reduce((s,x)=>s+x,0),g=v.filter(x=>x>0).reduce((s,x)=>s+x,0),l=-v.filter(x=>x<=0).reduce((s,x)=>s+x,0);return {trades:a.length,meanNet:sum/a.length,medianNet:qtile(v,.5),hit:a.filter(x=>x>0).length/a.length,pf:l?g/l:g?99:0,sumNet:sum};}
const allTrades=[];for(const e of dedup)for(const hold of HOLDS)for(const orientation of [1,-1]){const b=trade(e,hold,orientation,BASE_COST),s=trade(e,hold,orientation,STRESS_COST);if(b)allTrades.push({...b,scenario:'base'});if(s)allTrades.push({...s,scenario:'stress'});}
const families=[...new Set(dedup.map(e=>e.family))],candidates=[];
for(const family of families){const discEvents=dedup.filter(e=>e.family===family&&e.split==='discovery'),scores=discEvents.map(e=>e.score);for(const q of QS){const threshold=qtile(scores,q);for(const hold of HOLDS)for(const orientation of [1,-1]){const dbase=allTrades.filter(x=>x.family===family&&x.split==='discovery'&&x.hold===hold&&x.orientation===orientation&&x.scenario==='base'&&x.score>=threshold),dstress=allTrades.filter(x=>x.family===family&&x.split==='discovery'&&x.hold===hold&&x.orientation===orientation&&x.scenario==='stress'&&x.score>=threshold),vbase=allTrades.filter(x=>x.family===family&&x.split==='validation'&&x.hold===hold&&x.orientation===orientation&&x.scenario==='base'&&x.score>=threshold),vstress=allTrades.filter(x=>x.family===family&&x.split==='validation'&&x.hold===hold&&x.orientation===orientation&&x.scenario==='stress'&&x.score>=threshold),dm=metrics(dbase),ds=metrics(dstress),vm=metrics(vbase),vs=metrics(vstress);const eligible=dm.trades>=60&&vm.trades>=40&&dm.meanNet>0&&vm.meanNet>0&&ds.meanNet>-0.00025&&vs.meanNet>-0.00025&&(dm.pf??0)>1&&(vm.pf??0)>1;candidates.push({family,q,threshold,hold,orientation,discovery:dm,discoveryStress:ds,validation:vm,validationStress:vs,eligible,rank:(Math.min(dm.meanNet,vm.meanNet)+.5*Math.min(ds.meanNet,vs.meanNet))});}}}
const sleeves=candidates.filter(x=>x.eligible).sort((a,b)=>b.rank-a.rank);
function evalPortfolio(splitName,costScenario){const eventMap=new Map();for(const s of sleeves){for(const e of dedup){if(e.family!==s.family||e.split!==splitName||e.score<s.threshold)continue;const t=trade(e,s.hold,s.orientation,costScenario==='base'?BASE_COST:STRESS_COST);if(!t)continue;const k=`${e.symbol}|${e.t}`,p=eventMap.get(k);const score=s.rank+e.score*1e-6;if(!p||score>p.priority)eventMap.set(k,{...t,priority:score,sleeve:s});}}
  const es=[...eventMap.values()].sort((a,b)=>a.entryTime-b.entryTime||b.priority-a.priority),grouped=new Map();for(const e of es){const a=grouped.get(e.entryTime)??[];a.push(e);grouped.set(e.entryTime,a);}let equity=1,peak=1,maxDD=0;const active=[],busy=new Map(),used=new Map(),turn=new Map(),entries=new Map(),pnlDay=new Map(),trades=[];
  const day=x=>new Date(x*1000).toISOString().slice(0,10);function release(t){active.sort((a,b)=>a.exitTime-b.exitTime);while(active.length&&active[0].exitTime<=t){const p=active.shift(),pnl=p.entryEq*FRAC*p.net;equity+=pnl;peak=Math.max(peak,equity);maxDD=Math.max(maxDD,(peak-equity)/Math.max(peak,1e-9));const d=day(p.exitTime);turn.set(d,(turn.get(d)??0)+FRAC);pnlDay.set(d,(pnlDay.get(d)??0)+pnl);}}
  for(const [t,a] of [...grouped].sort((x,y)=>x[0]-y[0])){release(t);const d=day(t),u=used.get(d)??0;if(u>=ENTRY_BUDGET-FRAC/2)continue;let local=u;for(const e of a.slice(0,MAX_PER_STEP)){if((busy.get(e.symbol)??0)>t)continue;if(local+FRAC>ENTRY_BUDGET+1e-9||active.length*FRAC+FRAC>MAX_EXPOSURE+1e-9)break;active.push({exitTime:e.exitTime,entryEq:equity,net:e.net});busy.set(e.symbol,e.exitTime);local+=FRAC;turn.set(d,(turn.get(d)??0)+FRAC);entries.set(d,(entries.get(d)??0)+1);trades.push(e);}used.set(d,local);}const end=splitName==='evaluation1'?S3:END;release(end+20*STEP);const start=splitName==='evaluation1'?S2:S3,days=(end-start)/DAY,dayKeys=[];for(let t=Math.floor(start/DAY)*DAY;t<end;t+=DAY)dayKeys.push(day(t));const turns=dayKeys.map(d=>turn.get(d)??0),ents=dayKeys.map(d=>entries.get(d)??0),pd=dayKeys.map(d=>pnlDay.get(d)??0),nets=trades.map(x=>x.net),norm=equity>0?equity**(30/days)-1:-1,g=nets.filter(x=>x>0).reduce((s,x)=>s+x,0),l=-nets.filter(x=>x<=0).reduce((s,x)=>s+x,0);return {days,trades:trades.length,avgTradesPerDay:trades.length/days,totalReturn:equity-1,normalized30Return:norm,maxDD,avgDailyTurnover:turns.reduce((s,x)=>s+x,0)/turns.length,medianDailyTurnover:qtile(turns,.5),coverage45:turns.filter(x=>x>=4.5).length/turns.length,zeroTradeDays:ents.filter(x=>x===0).length,positivePnlDays:pd.filter(x=>x>0).length/Math.max(1,pd.filter(x=>x!==0).length),winRate:trades.length?trades.filter(x=>x.net>0).length/trades.length:0,meanTradeNet:trades.length?nets.reduce((s,x)=>s+x,0)/nets.length:0,pf:l?g/l:g?99:0,families:Object.fromEntries([...new Set(trades.map(x=>x.family))].map(f=>[f,trades.filter(x=>x.family===f).length]))};}
const e1=evalPortfolio('evaluation1','base'),e1s=evalPortfolio('evaluation1','stress'),e2=evalPortfolio('evaluation2','base'),e2s=evalPortfolio('evaluation2','stress');const promising=sleeves.length>0&&e1.meanTradeNet>0&&e2.meanTradeNet>0&&e1.normalized30Return>0&&e2.normalized30Return>0;
const availability=Object.fromEntries([...raw].map(([s,a])=>[s,{rows:a.length,first:a[0]?.time??null,last:a.at(-1)?.time??null}]));
const report={decision:promising?'FIVE_MIN_MICROSTRUCTURE_PROMISING':'FIVE_MIN_MICROSTRUCTURE_NOT_PROVEN',authority:'RESEARCH_ONLY_NO_DEPLOYMENT',goal:'find event-driven positive-edge sleeves that can contribute toward 5x daily turnover without forcing filler trades',periods:{start:new Date(START*1000).toISOString(),discovery:[new Date(START*1000).toISOString(),new Date(S1*1000).toISOString()],validation:[new Date(S1*1000).toISOString(),new Date(S2*1000).toISOString()],evaluation1:[new Date(S2*1000).toISOString(),new Date(S3*1000).toISOString()],evaluation2:[new Date(S3*1000).toISOString(),new Date(END*1000).toISOString()]},cost:{base:BASE_COST,stress:STRESS_COST},data:{interval:'5m',symbols:SYMBOLS,availability},method:'5m completed contract-stat state; 7d trailing standardized price/OI/taker/crowding/liquidation/funding features; predefined event families; discovery selects score quantile+direction+hold, validation must independently retain positive edge, only then fixed sleeves are evaluated on two untouched 30d blocks; execute next 5m mark; 15m family-symbol dedup',eventCount:dedup.length,candidateCount:candidates.length,eligibleSleeves:sleeves,evaluation:{evaluation1:{base:e1,stress:e1s},evaluation2:{base:e2,stress:e2s}},promising};writeFileSync('/tmp/5m-microstructure-events-report.json',JSON.stringify(report,null,2));console.log(JSON.stringify({decision:report.decision,periods:report.periods,eventCount:report.eventCount,candidateCount:report.candidateCount,eligibleSleeveCount:sleeves.length,eligibleSleeves:sleeves,evaluation:report.evaluation,promising},null,2));