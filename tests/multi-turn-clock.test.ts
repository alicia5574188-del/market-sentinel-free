import test from "node:test";
import assert from "node:assert/strict";
import {evaluateMultiTurnClock,MULTI_TURN_CLOCK_BAR_MS} from "../lib/multi-turn-clock.ts";
test("wall clock cannot consume missing candle data",()=>{const now=Date.parse("2026-09-22T12:06:31Z"),target=Math.floor((now-90_000)/MULTI_TURN_CLOCK_BAR_MS)*MULTI_TURN_CLOCK_BAR_MS;const r=evaluateMultiTurnClock({now,lastCycleAt:target-MULTI_TURN_CLOCK_BAR_MS+91_000,lastMarkAt:0,newestPathCompletedAt:target-MULTI_TURN_CLOCK_BAR_MS,allowDataCycle:true});assert.equal(r.dataDue,false);assert.equal(r.markDue,false);});
test("completed candle advances data clock",()=>{const now=Date.parse("2026-09-22T12:06:31Z"),target=Math.floor((now-90_000)/MULTI_TURN_CLOCK_BAR_MS)*MULTI_TURN_CLOCK_BAR_MS;assert.equal(evaluateMultiTurnClock({now,lastCycleAt:target-MULTI_TURN_CLOCK_BAR_MS+91_000,lastMarkAt:0,newestPathCompletedAt:target,allowDataCycle:true}).dataDue,true);});
test("critical clock may mark equity without consuming candle data",()=>{const now=Date.parse("2026-09-22T12:07:31Z"),target=Math.floor((now-90_000)/MULTI_TURN_CLOCK_BAR_MS)*MULTI_TURN_CLOCK_BAR_MS;const r=evaluateMultiTurnClock({now,lastCycleAt:target-MULTI_TURN_CLOCK_BAR_MS+91_000,lastMarkAt:target-MULTI_TURN_CLOCK_BAR_MS+91_000,newestPathCompletedAt:target-MULTI_TURN_CLOCK_BAR_MS,allowDataCycle:false});assert.equal(r.dataDue,false);assert.equal(r.fallbackMarkDue,true);});
