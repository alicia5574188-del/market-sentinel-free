name: Validate Funding Crowding Carry
on:
  pull_request:
    branches: [main]
    paths:
      - "scripts/research-funding-crowding-carry.mjs"
      - ".github/workflows/validate-funding-crowding-carry.yml"
permissions:
  contents: read
jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22.16.0
      - name: Check syntax
        run: node --check scripts/research-funding-crowding-carry.mjs
      - name: Run funding crowding carry audit
        run: node --max-old-space-size=4096 scripts/research-funding-crowding-carry.mjs
      - name: Print focused result
        run: jq '{decision,eventCount,selection,accepted,combined}' /tmp/funding-crowding-carry.json
      - uses: actions/upload-artifact@v4
        with:
          name: funding-crowding-carry
          path: |
            /tmp/funding-crowding-carry.json
            /tmp/funding-crowding-events.json
            /tmp/funding-crowding-trades.json
          retention-days: 7