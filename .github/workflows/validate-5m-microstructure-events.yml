name: Validate 5m Microstructure Events
on:
  pull_request:
    branches: [main]
    paths:
      - "scripts/research-5m-microstructure-events.mjs"
      - ".github/workflows/validate-5m-microstructure-events.yml"
permissions:
  contents: read
jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22.16.0
      - name: Check syntax
        run: node --check scripts/research-5m-microstructure-events.mjs
      - name: Run 5m microstructure event study
        run: node --max-old-space-size=4096 scripts/research-5m-microstructure-events.mjs
      - name: Print focused result
        run: jq '{decision,authority,goal,periods,cost,data,eventCount,candidateCount,eligibleSleeves,evaluation,promising}' /tmp/5m-microstructure-events-report.json
      - uses: actions/upload-artifact@v4
        with:
          name: 5m-microstructure-events-report
          path: /tmp/5m-microstructure-events-report.json
          retention-days: 7
