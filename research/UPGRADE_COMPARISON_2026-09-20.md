# 2026-09-20 上线前独立对照：证据范围与发布条件

用户条件：本次上线必须相比之前更优，否则无意义，确认可行就上线。“更优”不被擅自解释为用户明确要求净收益严格更高的保证。本轮按`AUDITED_REPAIR_RELEASE_GATE.md`的有限验收口径：具体正确性和资源改进得到验证、相关正常路径不退化，再结合完整Worker及准确构建发布检查作综合决定；不把单纯“测试通过”或合成案例说成收益保证。

## 本轮已执行结果

`upgrade-comparison-2026-09-20.json`记录21/21通过：7项确定性缺陷对照已改善，14项正常路径/规则生成对照保持一致。`correctnessImproved=true`、`normalPathsPreserved=true`；收益/回撤优越和全账户24小时容量认证仍未证明。脚本本身不批准部署，最终有限发布门由主任务综合判断，不将尚未认证的无限负载容量当成这次新增回归。报告的具体候选源码哈希与完整匿名对照结果保存在该JSON；它不是未来修订自动继承的发布凭证。

## 精确基线与命令

- 旧版固定为已上线 `401768ccd5ea75da6bf55b2855433aba2df5d1a5`；不使用更早的 `repo-parent`。
- 候选为运行对照命令时的工作树；结果记录相关生产源码 SHA-256，后续改动需要重跑。
- 执行：`node --experimental-strip-types research/upgrade-comparison-2026-09-20.mjs /path/to/snapshots`。
- 脚本从本地 Git 对象提取旧版纯模块，在同一进程用相同输入调用旧版和候选；不联网、不下单、不更改账户。
- `correctnessImproved` 与 `normalPathsPreserved` 只有各自所有对照通过才为 true。退出码只代表工程对照通过与否，不代表获准发布。
- `profitabilityImprovementVerified`、`drawdownImprovementVerified`、`releaseEligibilityUnderStrictProfitImprovement` 必须保持 false，除非另有真实可复现的绩效证据；当前脚本不会产生此类证据。
- `resourceSafetyVerified=false`专指尚未认证全账户24小时writes/requests/duration容量，不是说旧的“高水位写入抢走最后退出额度”反例仍存在。当前独立保护lane及实际Worker回归已处理该反例。
- `releaseEligible=false`的作用域是**本脚本不能单独批准部署**，不是当前综合发布必须HOLD；JSON的`releaseEligibilityScope`明确此区别。主任务依`AUDITED_REPAIR_RELEASE_GATE.md`结合资源、Worker及准确构建发布检查决定；不把无限负载认证临时添加为本次有限修复的硬门。

## 能验证的提升

| 同输入对照 | 旧版问题 | 候选验收条件 |
| --- | --- | --- |
| 盈利新高后重启 | 内存与重启结果不同 | 真实保护检查点 round-trip 后资金/持仓结果相同 |
| 两层组合减仓叠加 | 六笔全退出，多减一笔 | 计划剩余风险只扣一次，与单层目标一致 |
| 预警后数据减少 | LONG 入场重新放行、预算回升 | UNKNOWN 不作恢复证据，保留既有方向约束 |
| 连续下跌及后续真实回升 | 仍下跌时原方向预警解除 | 下跌保持受威胁方向保护；真实回升最终恢复 |
| 13根实际跨度120分钟 | 当成完整60分钟路径 | 缺口路径不得宣称fresh |
| LIVE现有风险99U再新增 | 源权威模式允许超100U | 1000U权益下拒绝新增，而正常比例单不变 |
| 前向存储失败/未运行 | 健康仍ready | 不再报告全链路就绪 |

以上不是历史事故次数或已挽回的美元收益。多平一笔的旧版可能恰好避开后续损失，较早保护也可能错过反弹；修复业务不变量不能证明所有价格路径上净利占优。

## 正常路径保留

脚本另外比较：正常1标的/8标的 LONG 和 SHORT 机会均仍可开仓，数量、报价成交价、名义额、杠杆、保证金、风险、入场费完全一致；普通持有、连续盈利、正常止损的财务结果一致；旧报价不能平仓；单调上涨和单调下跌状态/风险预算一致；正常预算内的LIVE镜像完全一致。

三份真实快照同一组保留测量与可见成交反馈输入，分别生成6、3、6项规则，新旧规则ID、条件、方向、期限、退出几何和净收益估计摘要应完全一致。此检查仅说明修复没有重写学习/规则选择，不能重建当时实际可交易候选，也不能证明实际全日频率不降低。

数据不足或持续逆势时不再放行受威胁方向，是预期的行为变化。其对真实机会、费用、净利及回撤的净影响仍需真实路径对照，不应声称“绝不漏掉盈利交易”。

## 为什么当前不能证明更赚钱

三份导出均早于401768c补丁。每份仅保留80笔平仓；去重177笔，累计428笔，缺251笔明细。每份1152条测量是学习样本，包含起点、期限、端点收益、期间上下极值和特征，不是连续完整OHLC与bid/ask报价tape。

即使部分端点能反推个别收盘价，也无法知道止损、最高点、回吐触发的先后顺序、观察间隔、价差和真实可执行成交价；不能把up/down极值拼成对候选有利的价格路径。快照也没有当时完整pending、逐轮入场可用性与全量执行归档。因此不能严格重放整个账户、更不能将缺失部分当零损益。

没有新旧同输入完整账户曲线，就不能证明扣费后收益更高、最大回撤更低、达到月翻倍进度，或分离行情暴露与真正策略增益。

## 必须独立通过的工程发布门

1. 所有本对照通过，且相关生产模块哈希与最终候选一致。
2. Worker全量账本/保护overlay原子提交、重启、陈旧overlay忽略、故障不发布未保存状态通过；纯函数检查不能替代此项。
3. 盈利高水位已有独立8640写/日lane，原金融8000写/日未被侵占；compact每个已测完整包省2行，空成交额游标一天1440次读取只写288次。实际Worker测试应验证最后金融退出额度不被peak占用。全账户仍需分别判断writes/requests/duration；当前保守写模型主账户63032、含两会员99192，不能等同全量24小时容量认证。详见资源盘点报告。
4. 原有金融、LIVE比例、手动开关、会员隔离、账户历史、幂等及发布必要回归通过；不因追求速度省略。
5. 本次有限修复发布按`AUDITED_REPAIR_RELEASE_GATE.md`的综合验收决定，本脚本不单独授权上线。若另要求扣费收益严格更高或回撤严格更低，当前证据不足：还需真实顺序输入与完整归档、冻结比较口径并完成相同输入的新旧重放；不能把工程正确性结果说成这种绩效证明。

## 盈利比较应怎样做

固定旧版、候选、起始账户和同一可执行输入序列；逐时推进，不用未来资料选币、选参数或修改退出。完整计入手续费、价差/滑点、资金费和未平仓净权益；保持丢包、缺口、观察延迟等相同。比较总净利、最大回撤、实际风险暴露、有效机会保留、换手/手续费、分币/分时段贡献及更高成本敏感性。按独立时间段而非同一行情波次的订单条数判断稳健性。

已看过的数据可作修复归因，不自动变成全新样本外验证。不得靠事后删除亏损币、强行反转亏损方向、零交易或挑一段上涨行情宣称优越；也不因此给生产添加用户已拒绝的长期影子晋级门槛。

当前独立结论：本轮已经有同输入的具体正确性改善与正常路径保持证据，资源改进及Worker验收另有对应测试；主任务可以据有限综合发布门判断可行性，本脚本不代为批准。未来收益更高尚未证明。若采用净收益严格更高的另一种验收口径，当前材料仍不足；这不是把该口径归因为用户已经明确提出的唯一上线条件。
