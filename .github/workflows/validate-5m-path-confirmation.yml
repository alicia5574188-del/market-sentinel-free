name: Validate 5m Path Confirmation
on:
  pull_request:
    branches: [main]
    paths:
      - "scripts/research-5m-path-confirmation.mjs"
      - ".github/workflows/validate-5m-path-confirmation.yml"
permissions:
  contents: read
jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22.16.0
      - name: Check syntax
        run: node --check scripts/research-5m-path-confirmation.mjs
      - name: Run 5m path confirmation study
        run: node --max-old-space-size=4096 scripts/research-5m-path-confirmation.mjs
      - name: Print focused result
        run: jq '{decision,authority,goal,periods,cost,shockCount,pathEventCount,candidateCount,eligibleSleeveCount,eligibleSleeves,nearMiss,evaluation,promising}' /tmp/5m-path-confirmation-report.json
      - uses: actions/upload-artifact@v4
        with:
          name: 5m-path-confirmation-report
          path: /tmp/5m-path-confirmation-report.json
          retention-days: 7
