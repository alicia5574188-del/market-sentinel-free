import { readFileSync, writeFileSync } from 'node:fs';

const DATASET=process.env.RESEARCH_DATASET??'/tmp/gate-history-passive-5m.json';
const OUTPUT=process.env.RESEARCH_OUTPUT??'/tmp/passive-overshoot-target5x.json';
const raw=JSON.parse(readFileSync(DATASET,'utf8'));
if(raw.interval!=='5m')throw new Error('requires Gate 5m candles');
const sourceDatasets=raw.datasets??[];if(sourceDatasets.length<12)throw new Error(`need >=12 symbols, got ${sourceDatasets.length}`);
const INITIAL=1000,DAY=86400000;
const split={discovery:[Date.UTC(2025,8,1),Date.UTC(2026,2,1)],validation:[Date.UTC(2026,2,1),Date.UTC(2026,5,1)],evaluation:[Date.UTC(2026,5,1),Date.UTC(2026,8,1)],full:[Date.UTC(2025,8,1),Date.UTC(2026,8,1)]};
const scenarios={base:{maker:.0002,taker:.0007,fillBuffer:.0002},stress:{maker:.0004,taker:.0009,fillBuffer:.0004},adverse:{maker:.0004,taker:.0014,fillBuffer:.0005},zeroStressFill:{maker:0,taker:0,fillBuffer:.0004}};
const sum=a=>a.reduce((s,x)=>s+x,0),mean=a=>a.length?sum(a)/a.length:0,monthKey=ms=>new Date(ms).toISOString().slice(0,7);
function lowerBound(a,x){let lo=0,hi=a.length;while(lo<hi){const m=(lo+hi)>>1;if(a[m]<x)lo=m+1;else hi=m;}return lo;}
function removeSorted(a,x){let i=lowerBound(a,x);while(i<a.length&&a[i]!==x)i++;if(i===a.length)throw new Error('rolling remove miss');a.splice(i,1);}function insertSorted(a,x){a.splice(lowerBound(a,x),0,x);}
function rollingFeatures(close,ranges,n){const len=close.length,ma=new Float64Array(len),med=new Float64Array(len);ma.fill(NaN);med.fill(NaN);if(len<=n)return{ma,med};let s=0;const sorted=[];for(let j=0;j<n;j++){s+=close[j];insertSorted(sorted,ranges[j]);}for(let i=n;i<len;i++){ma[i]=s/n;med[i]=sorted[Math.floor(n/2)];if(i+1<len){s+=close[i]-close[i-n];removeSorted(sorted,ranges[i-n]);insertSorted(sorted,ranges[i]);}}return{ma,med};}
const datasets=sourceDatasets.map(ds=>{const n=ds.rows.length,time=new Float64Array(n),open=new Float64Array(n),high=new Float64Array(n),low=new Float64Array(n),close=new Float64Array(n),ranges=new Float64Array(n);for(let i=0;i<n;i++){const r=ds.rows[i];time[i]=+r.time;open[i]=+r.open;high[i]=+r.high;low[i]=+r.low;close[i]=+r.close;ranges[i]=high[i]-low[i];}return{symbol:ds.symbol,n,time,open,high,low,close,f12:rollingFeatures(close,ranges,12),f24:rollingFeatures(close,ranges,24)};});
const mechanismKey=c=>[c.anchorBars,c.z,c.entryAtr,c.targetAtr,c.stopAtr,c.holdBars,c.orderLifeBars].join('|'),rawCache=new Map();
function rawTrades(config,scenarioName){const key=`${scenarioName}|${mechanismKey(config)}`;if(rawCache.has(key))return rawCache.get(key);const sc=scenarios[scenarioName],out=[];for(const d of datasets){const f=config.anchorBars===12?d.f12:d.f24;let i=Math.max(config.anchorBars,24);while(i<d.n-config.orderLifeBars-config.holdBars-2){const anchor=f.ma[i],atr=f.med[i];if(!(anchor>0&&atr>0)){i++;continue;}const z=(d.close[i]-anchor)/atr;if(Math.abs(z)<config.z){i++;continue;}const direction=z>0?-1:1,limit=d.close[i]-direction*config.entryAtr*atr;let fill=-1;for(let j=i+1;j<=i+config.orderLifeBars&&j<d.n;j++){const crossed=direction>0?d.low[j]<=limit*(1-sc.fillBuffer):d.high[j]>=limit*(1+sc.fillBuffer);if(crossed){fill=j;break;}}if(fill<0){i+=config.orderLifeBars+1;continue;}const entry=limit,target=entry+direction*config.targetAtr*atr,stop=entry-direction*config.stopAtr*atr;let exit=entry,exitIndex=fill,outcome='TIMEOUT',exitFee=sc.taker;const last=Math.min(d.n-1,fill+config.holdBars);for(let j=fill+1;j<=last;j++){const stopHit=direction>0?d.low[j]<=stop:d.high[j]>=stop,targetHit=direction>0?d.high[j]>=target*(1+sc.fillBuffer):d.low[j]<=target*(1-sc.fillBuffer);if(stopHit||targetHit){if(stopHit){exit=stop;outcome='STOP';exitFee=sc.taker;}else{exit=target;outcome='TARGET';exitFee=sc.maker;}exitIndex=j;break;}exit=d.close[j];exitIndex=j;outcome=j===last?'TIMEOUT':outcome;}const gross=direction*(exit-entry)/entry;out.push({symbol:d.symbol,openedAt:d.time[fill]*1000,closedAt:d.time[exitIndex]*1000,side:direction>0?'LONG':'SHORT',stopRate:Math.abs(entry-stop)/entry,grossReturnRate:gross,netReturnRate:gross-sc.maker-exitFee,outcome,signalZ:z});i=exitIndex+1;}}out.sort((a,b)=>a.openedAt-b.openedAt||Math.abs(b.signalZ)-Math.abs(a.signalZ));rawCache.set(key,out);return out;}
function portfolio(trades,fraction){let equity=INITIAL,peak=INITIAL,maxDD=0;const open=[],accepted=[];const settle=time=>{for(const t of open.filter(x=>x.closedAt<=time).sort((a,b)=>a.closedAt-b.closedAt)){equity+=t.netPnl;peak=Math.max(peak,equity);maxDD=Math.max(maxDD,(peak-equity)/Math.max(peak,1e-12));open.splice(open.indexOf(t),1);}};for(let i=0;i<trades.length;){const at=trades[i].openedAt;settle(at);const same=[];while(i<trades.length&&trades[i].openedAt===at)same.push(trades[i++]);for(const t of same){if(equity<=100||open.some(x=>x.symbol===t.symbol))continue;const notional=equity*fraction,plannedRisk=notional*(t.stopRate+.0014),gross=sum(open.map(x=>x.notional)),risk=sum(open.map(x=>x.plannedRisk)),sideGross=sum(open.filter(x=>x.side===t.side).map(x=>x.notional));if(gross+notional>equity*1.5||risk+plannedRisk>equity*.10||sideGross+notional>equity*1.0)continue;const a={...t,equityAtOpen:equity,notional,plannedRisk,netPnl:notional*t.netReturnRate};open.push(a);accepted.push(a);}}settle(Infinity);return{trades:accepted,endEquity:equity,maxDrawdown:maxDD};}
function metrics(account,start,end){const rows=account.trades.filter(t=>t.openedAt>=start&&t.openedAt<end),g=rows.filter(t=>t.netPnl>0),l=rows.filter(t=>t.netPnl<=0);let eq=INITIAL,peak=eq,dd=0;const monthly=new Map();for(const t of [...rows].sort((a,b)=>a.closedAt-b.closedAt)){const before=eq;eq+=t.netPnl;peak=Math.max(peak,eq);dd=Math.max(dd,(peak-eq)/Math.max(peak,1e-12));const k=monthKey(t.openedAt),m=monthly.get(k)??{start:before,end:before,trades:0};m.end=eq;m.trades++;monthly.set(k,m);}const days=(end-start)/DAY,turnover=2*sum(rows.map(t=>t.notional/Math.max(t.equityAtOpen,1e-12)))/days,months=[...monthly.values()].map(x=>x.end/x.start-1);return{trades:rows.length,tradesPerDay:rows.length/days,turnoverPerDay:turnover,returnPct:(eq/INITIAL-1)*100,avgMonthPct:(months.length?mean(months):-1)*100,positiveMonths:months.filter(x=>x>0).length,activeMonths:months.length,profitFactor:l.length?sum(g.map(t=>t.netPnl))/Math.abs(sum(l.map(t=>t.netPnl))):g.length?99:0,maxDrawdownPct:dd*100,targets:rows.filter(t=>t.outcome==='TARGET').length,stops:rows.filter(t=>t.outcome==='STOP').length,timeouts:rows.filter(t=>t.outcome==='TIMEOUT').length};}
function evaluate(config,scenarioName){const account=portfolio(rawTrades(config,scenarioName),config.fraction);return{endEquity:account.endEquity,maxDrawdownPct:account.maxDrawdown*100,periods:Object.fromEntries(Object.entries(split).map(([k,[a,b]])=>[k,metrics(account,a,b)]))};}
const mechanisms=[];for(const anchorBars of [12,24])for(const z of [1,1.5])for(const entryAtr of [.25,.5])for(const targetAtr of [.5,1])for(const stopAtr of [1,1.5])for(const holdBars of [6,12])mechanisms.push({anchorBars,z,entryAtr,targetAtr,stopAtr,holdBars,orderLifeBars:2});
const fractions=[.02,.025,.03,.04,.05],configs=mechanisms.flatMap(m=>fractions.map(fraction=>({...m,fraction})));
const rows=configs.map(config=>{const stress=evaluate(config,'stress'),d=stress.periods.discovery,inBand=d.turnoverPerDay>=3.5&&d.turnoverPerDay<=6.5,qualified=inBand&&d.returnPct>0&&d.profitFactor>=1.02&&d.maxDrawdownPct<=30,score=d.avgMonthPct*2-Math.abs(d.turnoverPerDay-5)*3-d.maxDrawdownPct*.1;return{config,stress,inBand,discoveryQualified:qualified,score};}).sort((a,b)=>Number(b.discoveryQualified)-Number(a.discoveryQualified)||Number(b.inBand)-Number(a.inBand)||b.score-a.score);
const targetRows=rows.filter(x=>x.inBand),discoveryCandidates=targetRows.filter(x=>x.discoveryQualified).slice(0,20);let selected=null;for(const row of discoveryCandidates){const v=row.stress.periods.validation;if(v.returnPct>0&&v.profitFactor>=1&&v.turnoverPerDay>=3.5&&v.turnoverPerDay<=6.5){selected=row;break;}}
const targetBandBest=[...targetRows].sort((a,b)=>b.stress.periods.discovery.avgMonthPct-a.stress.periods.discovery.avgMonthPct||b.stress.periods.discovery.profitFactor-a.stress.periods.discovery.profitFactor)[0]??null;
function diagnostics(row){if(!row)return null;return{config:row.config,stress:row.stress,base:evaluate(row.config,'base'),adverse:evaluate(row.config,'adverse'),zeroStressFill:evaluate(row.config,'zeroStressFill')};}
const targetBandDiagnostics=diagnostics(targetBandBest),selectedDetail=diagnostics(selected);let passes=false;if(selectedDetail){const s=selectedDetail.stress.periods,a=selectedDetail.adverse.periods;passes=s.discovery.avgMonthPct>=5&&s.validation.avgMonthPct>=5&&s.evaluation.avgMonthPct>=5&&s.evaluation.returnPct>0&&s.evaluation.profitFactor>=1&&s.full.turnoverPerDay>=3.5&&s.full.turnoverPerDay<=6.5&&a.discovery.returnPct>0&&a.validation.returnPct>0&&a.evaluation.returnPct>0;}
const report={research:'5m-passive-overshoot-fade-target5x-v2',authority:'RESEARCH_ONLY_NO_DEPLOYMENT',decision:passes?'FORWARD_VALIDATION_CANDIDATE':'PASSIVE_OVERSHOOT_TARGET5X_REJECTED',passes,data:{source:raw.source,sha256:raw.sha256,months:raw.months,symbols:raw.symbols},changeFromV1:'mechanism grid, fill rules, costs and splits are frozen. Only notional fractions are reduced from 10/20/30% to 2/2.5/3/4/5% so the dense mechanism is actually measured near the requested 5x/day turnover. A zero-fee scenario with the exact stress fill-through buffer isolates gross mechanism edge from fees.',costs:scenarios,configCount:configs.length,mechanismCount:mechanisms.length,targetBandCount:targetRows.length,discoveryQualified:rows.filter(x=>x.discoveryQualified).length,selected:selectedDetail,targetBandBest:targetBandDiagnostics,topTargetDiscovery:targetRows.slice(0,10),interpretation:passes?'Candidate survives target-sized stress/adverse tests; next step is broader-year robustness and paper fill validation.':targetBandDiagnostics?.zeroStressFill?.periods?.full?.returnPct<=0?'The target-sized mechanism is negative even with zero fees under the same conservative fill condition; close this passive overshoot family.':'Gross edge exists before fees but is insufficient after realistic maker/taker costs; any next route must improve transaction economics rather than tune these same signal thresholds.'};
writeFileSync(OUTPUT,JSON.stringify(report,null,2)+'\n');console.log('PASSIVE_TARGET5X_JSON='+JSON.stringify({decision:report.decision,passes,targetBandCount:report.targetBandCount,discoveryQualified:report.discoveryQualified,selected:report.selected&&{config:report.selected.config,stress:report.selected.stress.periods,adverse:report.selected.adverse.periods,zero:report.selected.zeroStressFill.periods},targetBandBest:report.targetBandBest&&{config:report.targetBandBest.config,stress:report.targetBandBest.stress.periods,base:report.targetBandBest.base.periods,adverse:report.targetBandBest.adverse.periods,zero:report.targetBandBest.zeroStressFill.periods},interpretation:report.interpretation}));
