name: Verify Read Only Equity Reference
on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/equity-stage.yml'
      - 'scripts/equity-apply.py'
      - 'scripts/equity-transfer-*.b64'
permissions:
  contents: write
jobs:
  stage:
    if: github.head_ref == 'feat/equity-curve-advisory' && github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22.16.0'
      - name: Apply exact locally verified bytes and remove transport helpers
        run: |
          mkdir -p /tmp/equity-verified
          cp scripts/equity-transfer-*.b64 /tmp/equity-verified/
          python scripts/equity-apply.py
          git rm --ignore-unmatch scripts/equity-transfer-1.b64 scripts/equity-transfer-2.b64 scripts/equity-transfer-3.b64 scripts/equity-apply.py .github/workflows/equity-stage.yml .github/workflows/equity-workspace.yml
      - name: Verify bounded history, raw statistics, auth and unchanged trading methods
        shell: bash
        run: |
          set -euo pipefail
          npm ci 2>&1 | tee /tmp/equity-verified/install.log
          npm run test:direct 2>&1 | tee /tmp/equity-verified/direct.log
          npm test 2>&1 | tee /tmp/equity-verified/test-build.log
          npm run typecheck 2>&1 | tee /tmp/equity-verified/typecheck.log
          npm run lint 2>&1 | tee /tmp/equity-verified/lint.log
          npx wrangler deploy --dry-run --config dist/server/wrangler.json 2>&1 | tee /tmp/equity-verified/dry-run.log
          git diff --check
          git add -A
          git diff --cached --check
      - name: Capture pre-release public health separately, no credentials or changes
        shell: bash
        run: |
          curl -sS --max-time 20 -o /tmp/equity-verified/health-before.json -w '%{http_code}' 'https://market-sentinel-free.alicia5574188.workers.dev/__health' > /tmp/equity-verified/health-status.txt
          date -u +'%Y-%m-%dT%H:%M:%SZ' > /tmp/equity-verified/time.txt
      - name: Store tested blobs only; connector controls reviewed branch publication
        uses: actions/github-script@v7
        with:
          script: |
            const fs=require('fs'),cp=require('child_process'),crypto=require('crypto');
            const {owner,repo}=context.repo,parent=context.payload.pull_request.head.sha,branch=context.payload.pull_request.head.ref;
            const current=await github.rest.repos.getBranch({owner,repo,branch});
            if(current.data.commit.sha!==parent)throw new Error('Head changed during verification');
            const base=await github.rest.git.getCommit({owner,repo,commit_sha:parent});
            const names=cp.execFileSync('git',['diff','--cached','--name-only','-z']).toString().split('\0').filter(Boolean),tree=[],sha256={};
            for(const path of names){
              if(!fs.existsSync(path)){tree.push({path,mode:'100644',type:'blob',sha:null});continue;}
              const bytes=fs.readFileSync(path),blob=await github.rest.git.createBlob({owner,repo,content:bytes.toString('base64'),encoding:'base64'});
              const expected=crypto.createHash('sha1').update(Buffer.concat([Buffer.from('blob '+bytes.length+'\0'),bytes])).digest('hex');
              if(blob.data.sha!==expected)throw new Error('Blob mismatch: '+path);
              tree.push({path,mode:'100644',type:'blob',sha:blob.data.sha});sha256[path]=crypto.createHash('sha256').update(bytes).digest('hex');
            }
            const result={parent,baseTree:base.data.tree.sha,tree,sha256,runId:context.runId,branchRefUnchanged:true,productionUnchanged:true};
            fs.writeFileSync('/tmp/equity-verified/verified-tree.json',JSON.stringify(result,null,2));console.log(JSON.stringify(result));
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: equity-verified-${{ github.run_id }}
          path: /tmp/equity-verified/
          retention-days: 7
          if-no-files-found: error
