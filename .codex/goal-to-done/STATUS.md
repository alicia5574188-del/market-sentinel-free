# Status

Done on production at commit `7fc1c425a8ffffc66938937b45083feb7e9c3d1e`. GitHub Actions run #538 completed verify, deploy, and monitor successfully, including advancing PAPER authority, schema/credential-presence checks, homepage/history smoke tests, and the actual Gate futures candle proof.

The authoritative universe is permanently BTC, ETH, and SOL; checkpoint recovery removes ZEC and every prior rotating symbol. Each market has an independent freshness decision, so one recovering feed no longer labels every coin incomplete.

Follow-up deployed at commit `2bd3200ddcc5a604656f06e2d9b3632d3700398d`; GitHub Actions run #539 completed successfully. The runtime API was healthy and all three markets reported both primary and ancillary evidence fresh. The false `数据恢复中` display was traced to the browser treating a single request lasting over five seconds as a total market failure. The UI now allows a 30-second request and retains a verified runtime snapshot for 90 seconds across transient transport delay; backend per-market freshness remains authoritative and still blocks stale-price entry. Production browser verification now shows `后台运行中`, BTC and SOL `继续观察`, ETH `等待进场`, live prices, and all three 72-candle charts.

Second follow-up deployed at commit `79f9bdaff22ace37622fad8d78f8c8ca228deb10`; GitHub Actions run #540 completed successfully. Transport freshness is now presentation-only: a slow phone refresh shows a page-delay notice while preserving the last authoritative per-market state, and only actual backend `stale`, `fresh`, or `ancillaryFresh` failures produce `数据恢复中`. Order cards now identify `notional` as PAPER futures contract notional and show a display leverage tier plus estimated margin, with an explicit note that trigger-time price/equity/risk are recalculated and no Gate order is submitted. Production verification showed backend running, BTC observing, ETH and SOL waiting to trigger, all three candle charts, and order-card values including 10×/391.49 U and 20×/534.62 U.

Commit `76070bd8fe74f2117d914670a2ffa83fe0d9287b` deployed successfully in run #541 in 2m38s, down from #540's 3m12s. It fixes the zero-fill plan lifecycle found from production evidence: the former 90-second plan expired before distant predictive entries could be reached, neutral or slightly drifting two-second decisions cancelled frozen intent, and sub-friction targets appeared in Orders only to fail trigger-time economics. Plans now freeze for 15 minutes, survive neutral/same-side drift, and pass fee/slippage economics before display. A neutral-tick crossing is directly tested to open PAPER. Commit `683e70c7d5d6528dde83c02bc4d5450f8d0bb460` then deployed successfully in run #543, making all card levels use the frozen plan and serializing scheduled monitoring with deployments. Production acceptance after warmup showed `后台运行中`, three executable positive-R:R plans, fixed bottom navigation, tab selection at scrollY 0, consistent card/execution/chart levels, and all three plan levels unchanged across a further 35-second live observation. Local typecheck, lint, 53 direct tests, 12 architecture/migration tests, build, and diff check pass.

Each card displays actual completed Gate USDT futures candles with working 1m, 15m, and 1h switches. MarketStream mirrors its own nine strategy candle sets to the existing D1 settings row at most once per five minutes, keeping chart reads independent from the two-second trading loop. The chart contains candles, liquidity regions, and strategy/position levels; adjacent Chinese text states direction and whether the system is waiting for real-time price, forming a plan, or holding. A failed period switch can never relabel an older period.

Production visual acceptance observed `后台运行中`, exactly three market cards, 216 rendered candles, no ZEC, and distinct BTC 1m versus 1h OHLC values. The release remains physically PAPER-only; the public LIVE control is visible but security-locked and no Gate mutation endpoint exists.

Commit `cba470f233121875b6ccb382dcf772c6e5314345` deployed successfully in GitHub Actions run #544 in 2m25s. It fixes the remaining direction-flip race for the shared BTC/ETH/SOL state machine: an opposite two-second recalculation can no longer cancel or replace a PREPARED plan, and a fresh trigger crossing is preserved through the runtime's planning pass so the execution pass opens the original frozen side. Two SOL-shaped regression tests cover both approach and exact crossing. Local direct tests pass 55/55, architecture/migration tests pass 12/12, and build, typecheck, lint, and diff check pass. Production reports `后台运行中`; BTC and ETH kept identical frozen side/trigger/stop/target levels across another 25-second observation while live prices and liquidity evidence continued updating.

The current release candidate adds the owner-controlled Gate live layer without forking the strategy. The owner logs in as `owner` using the existing backend access code, receives an eight-hour signed HttpOnly session, and alone can turn live execution on or off. Live starts OFF after the upgrade. Enabling it reuses the frozen BTC/ETH/SOL plan levels and dynamic position management: BREAKOUT uses a Gate price-trigger market entry, REVERSAL/RANGE use GTC limit entries, and every fill receives a close-only structural stop. Disabling cancels unfilled entries while existing positions retain exchange protection and strategy exits. Pending entries and open positions share the same 5% account risk cap, same-cycle margin is reserved, ambiguous submissions are reconciled before any retry, and unknown Gate positions/orders or dual-side mode fail closed. The existing encrypted live credential row remains untouched. Local typecheck, lint, 62 direct tests, 12 architecture/migration tests, production build, and whitespace validation pass.

Commit `5e47df241266e71cf0e43715aab0c7a2dd6b65b9` deployed successfully in GitHub Actions run #546. It fixes the real-order rollback defect observed after a failed enable. All BTC/ETH/SOL live intents are now sized and checked as one batch before the first Gate mutation. If submission later fails, LIVE is forced OFF and every system-tagged partial entry is reconciled and cancelled. Turning LIVE off always performs the same exchange reconciliation, even when runtime memory contains no pending entries; an authenticated manual cleanup button exposes this recovery without requiring another enable attempt, and never touches untagged manual orders. The dashboard now has separate Live Account, Live Orders, and API Management views. Owner API save/replace validates Gate read-only and encrypts server-side without enabling LIVE; deletion checks that LIVE is off and Gate is flat with no orders. The scheduled monitor accepts the intentional zero-credential state and still verifies the credential API is unauthorized publicly. Local typecheck, lint, 64 direct tests, 12 architecture/migration tests, production build, and whitespace validation pass. Production acceptance verified advancing health, the homepage/history/candle feeds, public runtime redaction, owner auth configuration, and unauthenticated rejection of the credential endpoint.

Commit `07b5b01293beb6f1c44d307961639f1dd506f61c` deployed successfully in GitHub Actions run #547. The decision hero, PAPER equity/PnL/position/risk cards, and PAPER transport notices now belong only to the Brain tab. Orders, Live, History, and Settings begin directly with their own content while the iPhone navigation remains fixed at the bottom.

Commit `fa6ab15d9048fef576c9a5c7ecdfa6eda42ab60c` deployed successfully in GitHub Actions run #548. It fixes the Gate cleanup failure found on the live account page. Gate int64 order IDs are preserved as exact strings instead of passing through lossy JavaScript numbers. Both closing LIVE and the authenticated stale-order cleanup now cancel system entry orders from a fresh exchange snapshot, reread Gate after cancellation, retry once if necessary, and fail visibly while keeping LIVE off if Gate still reports an order. Manual orders and protective stops remain untouched. Local typecheck, lint, 66 direct tests, 12 architecture/migration tests, production build, and whitespace validation pass. Production health is ready with advancing authority and BTC/ETH/SOL only.
