# 哨兵量化原件

**项目正式名称：Market Sentinel / 哨兵量化**  
**原件定版时间：2026-08-25（北京时间）**  
**权威部署仓库：`alicia5574188-del/market-sentinel-free`**  
**程序代码原件提交：`16df8b842678fc976b7e72731f8afe6e9158a16d`**  
**只读留档基线分支：`sentinel-original-baseline-2026-08-25`**  
**正式程序地址：`https://market-sentinel-free.alicia5574188.workers.dev/`**  
**健康接口：`https://market-sentinel-free.alicia5574188.workers.dev/__health`**

---

## 0. 本文件的地位

从本文件建立之日起，**“哨兵量化原件”就是以后所有更新、优化、重构、风控调整、界面调整和 Gate 实盘功能扩展的唯一项目基线说明**。

以后继续开发时遵守以下规则：

1. 不再从旧 ChatGPT Site、旧模拟站、旧 TestNet 方案或更早的仓库状态重新开始。
2. 当前程序代码原件固定为提交 `16df8b842678fc976b7e72731f8afe6e9158a16d`。
3. 该代码额外保存在 `sentinel-original-baseline-2026-08-25` 分支。
4. 以后新功能只能在此基础上继续增加或修改。
5. 如果未来新版本出现严重问题，可以准确回退到这个原件基线。
6. 不允许为了修一个问题，把已经验证过的后台运行、安全边界、风控、Gate 密钥保护和订单保护机制无意删除。
7. API Key、API Secret、OWNER_ACCESS_TOKEN、密码、验证码等秘密不得写进本文件、源码或聊天记录。
8. 用户只负责必须本人完成的账户授权、安全确认和最终使用判断；普通开发、查错、代码修改和验证由开发侧完成。
9. 用户希望以后优先只看到最终结果：问题是否解决，以及需要做什么，不再接收大量开发过程细节。

---

# 1. 项目最初目标

项目最初不是为了做一个简单均线机器人，而是为了做一套：

**以 iPhone 为主要控制端、服务器 24 小时独立运行、监测 Gate USDT 永续合约全市场、只有在完整证据满足时才确认交易机会，并最终可以接入小额 Gate 实盘自动执行的量化交易系统。**

最初原则：

- 监控 Gate 成交活跃的 USDT 永续合约。
- 重点关注核心币和流动性良好的合约。
- 综合多周期趋势、价格结构、动量、成交量、波动率、资金费率、基差、OI、清算、订单簿、Spot CVD、BTC/ETH 环境等证据。
- 只有完整入场条件成立才确认信号。
- 每个信号必须包含方向、触发、入场区、止损、TP1、TP2、失效条件、支持证据和反证。
- 同币同一时刻只允许一个有效交易生命周期。
- 不刷量、不自成交、不为了产生订单而交易。
- 允许空仓，没有好机会就不交易。
- 每笔订单必须完整复盘。
- 页面关闭后后台仍持续监测和管理订单。

---

# 2. 第一阶段：模拟交易与信号系统

最开始不直接接真实 Gate 自动交易，而是先建立模拟执行系统。

早期模拟账户按 1,000U 设计，用来验证：

- 信号是否按规则生成。
- 是否追涨杀跌。
- 是否使用未收盘 K 线。
- 同币是否重复开单。
- 止盈止损是否执行完整。
- 页面关闭后后台是否继续运行。
- 订单历史和复盘是否持久化。
- 策略是否真正积累样本，而不是用演示数据假装有效。

交易状态逐步形成：

- observing
- pre-alert
- confirmed / holding
- blocked
- closed / cooldown

订单生命周期必须是：

**确认 → 持仓 → 明确退出 → 平仓 → 复盘 → cooldown**

主要记录：入场价、止损、TP1、TP2、数量、名义价值、保证金、杠杆、计划风险、成本、实际盈亏、MFE、MAE、持仓时间、退出原因、证据变化和结构化经验。

TP1 后止损移动到保护价。模拟止损按已经保存的触发价结算，避免后台轮询延迟造成不合理的更差成交假设。

---

# 3. 连续亏损后对信号引擎的大修

早期真实观察中出现用户指出的连续亏损案例，包括 HYPE 多、PUMP 多、BEAT 空。

回查后确认旧逻辑存在真实缺陷：

1. 使用尚未结束的 5 分钟 K 线。
2. RSI 已接近极端区域时仍把动量当强延续证据。
3. 极端同向清算和已经发生的大幅 24 小时位移仍可能被当成顺势证据。
4. 完整 K 线成交量不足时，其他高度相关指标仍可能补分让信号过关。

修正后：

- 5m / 15m / 1h / 4h 指标只使用已完成 K 线。
- 完整 5m 方向必须通过。
- 完整 K 线量能必须通过。
- RSI / EMA / 24h 防追价必须通过。
- 清算耗竭必须通过。
- 这些硬闸门不可由其他指标补分绕过。
- RSI > 72 或 RSI < 28 视为耗竭风险，不再提供最大延续加分。
- 已有明显日内位移又出现极端同向清算时禁止追单，等待新结构。
- 为 HYPE / PUMP / BEAT 等暴露问题建立确定性回归测试。

由此形成原则：**评分可以综合，安全闸门不能补分绕过。**

---

# 4. 旧订单与数据库污染问题

旧 Site 曾把观察信号错误回填成有杠杆、有保证金的订单，导致页面出现并非完整交易生命周期产生的历史记录。

处理：

- 旧 `contract_v2_recalculated` 记录隔离为 `legacy_signal_v1`。
- 不再赋予这些旧记录杠杆、保证金、数量、强平价或 USDT 盈亏。
- 不进入当前正式订单列表、模拟账户统计和策略学习。
- 新 Cloudflare 环境使用新的 D1，从干净数据开始。

原则：**只有真实经历“确认 → 持仓 → 平仓”的订单，才进入统计和学习。**

---

# 5. iPhone 与 UI 阶段

项目从一开始以 iPhone 为主要使用场景。

完成：

- iPhone 单列布局。
- 桌面双列布局。
- 固定底部导航。
- PWA metadata。
- Service Worker。
- 手机下拉刷新和整体数据刷新。
- 机会、雷达、订单、实盘、设置等导航。
- 后台状态、行情、持仓、订单、实盘状态可视化。

曾出现底部导航随滚动、页面结构错位等问题，后续通过调整滚动容器和固定 viewport 解决。

---

# 6. 从 ChatGPT Site 迁移到 Cloudflare

旧版本曾运行在：

`https://market-sentinel.alicia5574188.chatgpt.site`

为了真正实现页面关闭仍运行、后台扫描、持仓持续监控、D1 持久化和未来 Gate 实盘，项目迁移到 Cloudflare。

最终权威环境：

- Worker：`market-sentinel-free`
- 正式地址：`https://market-sentinel-free.alicia5574188.workers.dev/`
- 正式部署仓库：`alicia5574188-del/market-sentinel-free`

旧 ChatGPT Site 不再作为权威运行环境，不允许以后把旧站数据库、身份、历史订单和新 Worker 混在一起。

---

# 7. Cloudflare 后台架构

### Cloudflare Worker

负责 SSR、API、登录边界、静态资源、健康接口、Gate 私有 API 请求入口和实盘协调入口。

### D1

持久化设置、行情扫描、信号、模拟订单、复盘、策略学习、推送订阅、Gate 加密凭据、实盘控制状态、实盘订单和审计。

### Durable Objects

`PositionMonitor`：有持仓时约每 10 秒检查，更新持仓并执行保护。  
`MarketScanner`：约每 60 秒初筛市场，在免费额度内深度分析优先标的。

### Cron

每分钟检查和恢复 Durable Object alarms，不依赖 iPhone 页面保持打开。

### Web Push

页面关闭后仍可发送服务器端事件通知。

---

# 8. Cloudflare 部署过程中解决的关键故障

## 8.1 D1 Binding 重复

曾出现 `Bindings must have unique names`。

原因：Cloudflare Vite 配置合并时，本地占位 DB 与真实 DB 被重复拼接，`nodejs_compat` 也可能重复。

修复：只在缺少 DB 时补占位，对 compatibility flags 去重，并加入 binding 回归测试。

## 8.2 页面变成裸文字、CSS/JS 404

曾出现后端正常，但 iPhone 页面无样式、客户端 hydration 失败。

原因：Worker 把 `/assets/*` 错误交给应用路由，导致 Vite hash CSS/JS 404。

修复：app router 前先调用 `env.ASSETS.fetch(request)`；真实静态文件先返回，不存在才进入应用路由。

修复后 CSS、JS、manifest、service worker、favicon 恢复。

---

# 9. GitHub → Cloudflare 正式部署链

当前正式链路：

`market-sentinel-free/main` → Cloudflare 自动部署 → 正式 workers.dev 地址。

原始/模板仓库为 `alicia5574188-del/market-sentinel`；以后正式实盘程序只以 `market-sentinel-free` 为权威仓库。

---

# 10. 从模拟转向 Gate 实盘

随后用户明确要求开始真正做实盘程序。

核心要求：

- 不接受每一单人工确认。
- 需要自动实盘总开关。
- OFF：不创建新的真实订单。
- ON：策略可以自动开仓并管理止盈止损。
- API Key / Secret 直接在程序里填写保存。
- 不使用 Gate 子账户。
- 交易所只放准备用于量化的小额资金。
- 不再额外保留 TestNet 使用流程。
- 固定 15U 利润门槛必须改成按本金/权益比例。

这一步把项目从模拟监测 PWA 推进成可控的 Gate 小额实盘执行系统。

---

# 11. TestNet 的取舍

最早实盘版本采用 TestNet-first。

用户明确认为已经有模拟模式，再增加 TestNet 过于重复，而且只准备使用小额实盘资金测试。

最终：

- UI 不再提供 Gate TestNet。
- 新凭据只允许 Gate 实盘。
- 旧 TestNet 凭据即使遗留，也不能开启自动实盘。
- 模拟模式继续承担策略验证。
- Gate live 承担真实小额执行。

---

# 12. Gate API 凭据安全设计

最终流程：

1. 浏览器填写 API Key / Secret。
2. 浏览器不长期保存 Secret。
3. 后端调用 Gate 私有接口验证。
4. 验证通过后使用 AES-GCM 加密。
5. 密文写入 D1。
6. UI 后续只显示掩码/Key hint。
7. 后端不把原始 Secret 返回浏览器。
8. 保存 API 不会自动开启实盘。
9. 更换后台访问密钥后需要重新填写 Gate 密钥。
10. 不允许提现权限。

Gate API Secret 不应发送到聊天中。

---

# 13. Cloudflare `Illegal invocation` 故障

第一次真实验证 Gate API 时出现：

`Illegal invocation: function called with incorrect 'this' reference`

原因：Gate 私有客户端把 Cloudflare host-provided `fetch` 保存成类成员后，通过 `this.fetcher(...)` 调用，Cloudflare receiver 不正确。

修复：把 fetch 正确绑定到 Worker 全局环境，并增加回归测试。

---

# 14. Gate futures 权限与 account 权限

随后出现：

`FORBIDDEN: Request API key does not have account permission`

原因：程序验证时额外调用 `/account/detail`，而用户只按要求给了永续合约 futures 权限。

最终修复：

- futures 合约账户接口是必须验证项。
- `/account/detail` 变成可选信息。
- 没有 account 权限不再拒绝保存。
- futures-only API 可以完成验证。
- 不要求钱包、提现或无关写权限。
- 增加 futures-only 权限回归测试。

---

# 15. Gate 单向持仓模式

Gate 验证成功后检测到账户是双向持仓模式。

本程序最终要求 Gate 使用**单向持仓模式**。

含义：同一个合约不能同时保留多仓和空仓；不同币种完全可以方向不同，例如 BTC LONG、ETH SHORT、SOL LONG。

程序增加“切换为单向模式并继续保存”。

安全边界：

- 必须由所有者点击确认。
- 不会后台偷偷修改 Gate 设置。
- 切换前必须确认无持仓、无挂单。
- 有持仓或挂单则拒绝切换。
- 增加 position mode 安全测试。

---

# 16. 自动实盘总开关

### 自动实盘关闭

- 不创建新的 Gate 订单。
- 已有仓位和保护逻辑继续后台对账。
- 保存 API 不等于开启交易。

### 自动实盘开启

只处理本次开启之后出现的新确认信号。所有实盘关键动作留审计记录。

---

# 17. 固定 15U 风控被淘汰

早期曾要求 TP2 预计净利润至少 15U。用户指出固定金额不能适应不同本金。

最终所有核心资金风控改为账户权益比例：

| 风控项目 | 原件规则 |
|---|---:|
| 单笔计划最大亏损 | 当前权益 1% |
| TP2 最低预计净利润 | 当前权益 1.5% |
| 单笔最大保证金占用 | 当前权益 20% |
| UTC 当日已实现亏损暂停线 | 参考权益 3% |
| 实盘权益峰值回撤锁定 | 峰值权益 10% |
| 同时最多实盘仓位 | 3 |
| 同一方向最多实盘仓位 | 2 |

这套规则覆盖模拟、实盘开仓前、Gate 实际张数复核、成交后保护和后台恢复复核。

以后禁止重新把固定 10U / 15U 作为核心本金风险阈值。

---

# 18. Gate 账户权益与逐仓可用额度

当前 Gate API 中：

- `total + unrealized PnL` 用于账户权益口径。
- `available` 用于逐仓可用额度。
- `cross_available` 是全仓可用口径。

本程序实盘开仓前会设置 isolated / 逐仓，因此逐仓订单保证金上限只能使用 `account.available`。

曾发现旧代码用 `Math.max(account.available, account.cross_available)`，可能把全仓额度错误用于放大逐仓订单。

已修为：

- 逐仓下单只使用 `account.available`。
- 增加专门回归测试。
- UI 明确“验证时逐仓可用”为验证快照，不能把它当成账户总权益。

---

# 19. 实盘旧信号保护

为了防止 Worker 延迟、队列、重启或部署后拿几分钟前的旧信号补开仓：

**实盘候选最大年龄 = 2 分钟。**

还必须满足信号发生在本次自动实盘开启之后。

---

# 20. 新版本部署自动关闭新开仓

实盘开关会持久化到 D1。为了避免新代码部署后沿用旧 ON 状态直接继续下单，加入 Cloudflare Worker Version Metadata 边界。

真正发布新版本时：

- 自动关闭新开仓。
- 已有仓位保护、止盈止损和对账继续。

普通 Worker 重启或网页刷新不会被误判成新版本部署。

---

# 21. 组合仓位风险

总实盘仓位最多 3 个。

为了避免三个仓位全部同方向受同一次市场反转影响，又增加：

**同一方向最多 2 个实盘仓位。**

这与 Gate 的单向持仓不是一回事：Gate 单向限制同一个合约；程序方向上限限制整个组合不要全部压一个方向。

---

# 22. 近期表现闸门与连续亏损保护

当前原件加入动态表现保护：

### Gate 实盘连续亏损

最近连续 2 笔 Gate 实盘亏损 → 暂停新开仓 6 小时；已有仓位继续保护。

### 模拟连续亏损

最近模拟连续 3 笔亏损 → 暂停实盘新开仓；模拟继续运行，表现恢复后自动放行。

### 模拟近期胜率

观察最近最多 8 笔模拟结果。至少 6 个样本后，如果胜率 < 40% 且累计净结果 <= 0，则暂停实盘新开仓。

### 实盘平仓盈亏尚未归因

最近一笔 Gate 实盘已平仓、但后台还没有完成真实已实现盈亏归因时，不允许立即继续开下一单。

---

# 23. 实盘订单执行与保护原则

当前原件坚持：

- 只有自动实盘 ON 后的新确认信号才能进入实盘。
- 同币不能重复创建活动订单。
- 下单前重新读取 Gate 合约、行情、账户、仓位和挂单。
- 实时价格偏离策略入场区过多则拒绝。
- 使用 Gate 实际合约乘数、最小/最大张数和 mark price。
- 检查合约状态/下架状态、手续费和滑点。
- 按 Gate 实际张数重新计算名义价值、保证金、止损风险和 TP2 净利润。
- 开仓前设置 isolated leverage。
- 止损/止盈使用 Gate 保护机制。
- 保护失败时 fail closed。
- 关键平仓/减仓使用 reduce-only。
- 程序订单使用自己的 client text 前缀，便于识别孤儿单。
- Worker 后台持续对账。
- API/网络/订单状态异常不能静默继续开新仓。

---

# 24. 一键停机

实盘页保留一键停机能力，用于：

- 关闭新开仓。
- 撤销本程序相关挂单。
- 使用 reduce-only 清理需要处理的仓位。
- 写入审计。

这是最高优先级安全入口，未来 UI 重构不能删除。

---

# 25. 实盘 UI 状态语义修复

用户发现进入“实盘”页后顶部仍显示“无交易权限”。实际只是未保存 Gate API。

最终状态语义改为分开表达：

- Gate 行情实时
- 实盘未配置
- 自动实盘关闭
- 自动实盘开启
- API 异常（需要时）

原则：行情连接、API 配置和自动交易授权不能混为一个状态。

---

# 26. GitHub Actions 邮件循环事故

开发过程中为了远程验证曾临时创建 GitHub Actions。

其中一次 workflow 由 push 触发，同时内部又修改并 push，造成递归触发和连续失败邮件。

最终：

- 删除临时 workflow。
- 清理 `.github/workflows` 临时验证文件。
- 后续不再使用会因代码修改递归触发邮件的临时 Actions。
- GitHub 不再作为测试日志输出器。

这是以后开发必须遵守的流程纪律。

---

# 27. 当前测试体系

原件已有测试覆盖：

- signal engine
- trade lifecycle
- risk policy
- contract simulation
- Gate public/private client
- global risk
- Web Push
- background scheduler
- auth / owner access
- live entry freshness
- live deployment safety
- live portfolio risk
- live performance gate
- Cloudflare Gate fetch binding
- Gate futures-only permission
- Gate position mode
- Gate isolated available
- live trading
- migration smoke
- rendered HTML
- mobile navigation
- responsive layout
- auth boundary
- Cloudflare deployment config
- live trading UI

原则：**构建通过不等于实盘正确，资金和订单边界必须有确定性回归测试。**

---

# 28. 当前主要源文件职责

- `worker/index.ts`：Worker、静态资源、Cron、Durable Object、健康接口。
- `lib/gate-client.ts`：Gate 公共市场数据。
- `lib/gate-private.ts`：Gate 私有签名请求、账户、仓位、订单、条件单、API 验证。
- `lib/gate-position-mode.ts`：Gate 单向持仓安全切换。
- `lib/live-risk.ts`：实盘开仓计划、权益、保证金、手续费、滑点、TP2、止损和账户风险。
- `lib/risk-policy.ts`：权益比例核心风控。
- `lib/live-entry-freshness.ts`：2 分钟实盘新鲜度保护。
- `lib/live-deployment-safety.ts`：Worker 新版本部署关闭新开仓。
- `lib/live-portfolio-risk.ts`：组合方向风险。
- `lib/live-performance-gate.ts`：近期表现和连续亏损冷却。
- `lib/live-trading-engine.ts`：真实 Gate 执行、保护、对账、停机和订单生命周期核心。
- `lib/live-trading-repository.ts`：D1 实盘凭据、控制状态、订单、审计和性能数据。
- `lib/credential-vault.ts`：Gate API 凭据加密/解密。
- `lib/scanner.ts`：市场扫描和交易生命周期入口。
- `lib/signal-engine.ts`：信号证据、反证和硬闸门。
- `lib/trade-lifecycle.ts`：模拟止盈止损、平仓和复盘。
- `app/page.tsx`：主 UI。
- `app/api/live/*`：credentials、control、status、reconcile、emergency、position-mode。

---

# 29. 当前正式程序的安全不变量

未来任何更新都不能无意破坏：

1. 自动实盘默认关闭。
2. 保存 Gate API 不会自动开启交易。
3. 新版本部署自动关闭新开仓。
4. 已有仓位保护不会因关闭新开仓而停止。
5. API Secret 不返回浏览器、不写源码、不写文档。
6. 不要求提现权限。
7. 不要求无关账户写权限。
8. futures-only API 可以验证。
9. Gate 使用单向持仓。
10. 切换单向前必须空仓、无挂单。
11. 实盘新信号最大年龄 2 分钟。
12. 同币不可重复开活动订单。
13. 最多 3 个实盘仓位。
14. 同方向最多 2 个。
15. 单笔止损风险 <= 权益 1%。
16. TP2 最低净利润 >= 权益 1.5%。
17. 单笔保证金 <= 权益 20%。
18. UTC 当日已实现亏损达到参考权益 3% 后暂停。
19. 权益较峰值回撤达到 10% 后锁定新开仓。
20. Gate 实盘连续 2 笔亏损后冷却 6 小时。
21. 模拟连续 3 笔亏损后暂停实盘。
22. 近期模拟至少 6 笔后，胜率 < 40% 且累计不盈利则暂停实盘。
23. 最新实盘平仓盈亏尚未完成归因时不开下一单。
24. 逐仓订单只按 Gate `available` 做保证金上限。
25. 不能用 `cross_available` 放大逐仓订单。
26. 下单后必须维持保护单/保护逻辑。
27. 关键减仓/平仓使用 reduce-only 等安全语义。
28. 所有实盘关键动作保留审计。
29. 不再使用递归 GitHub Actions 给用户制造连续邮件。

---

# 30. 当前产品状态

截至原件定版：

- Cloudflare 正式 Worker 已作为权威生产环境。
- GitHub 正式部署仓库已建立。
- D1 持久化已建立。
- Durable Object 后台监控/扫描架构已建立。
- 模拟策略继续积累结果。
- Gate 实盘 API 已完成程序内填写、验证、加密保存流程。
- futures-only 权限兼容已修复。
- Cloudflare fetch `Illegal invocation` 已修复。
- Gate 单向持仓模式已支持安全切换。
- 实盘自动交易有独立总开关。
- 当前自动实盘保持关闭，用户确认程序后再决定是否开启。
- 核心风控已经从固定 U 迁移为权益比例。
- 逐仓可用额度口径与真实下单方式统一。
- 旧信号、部署、组合方向、连续亏损和近期表现保护已经加入。

---

# 31. 与盈利能力的边界

本原件是一个已经具备真实 Gate 执行能力和多层风控的量化程序基线，但：

- 软件测试通过 ≠ 策略必然盈利。
- 风控正确 ≠ 信号长期一定有正期望。
- 未来核心优化方向仍然是用真实完整样本验证信号质量。
- 不允许因为短期盈利删除硬风控。
- 不允许因为短期亏损随意曲线拟合。
- 策略调整应尽量使用滚动统计、walk-forward、分市场状态、消融测试、MFE/MAE、成本后结果、最大连续亏损和样本外表现。

---

# 32. 未来更新的标准工作方式

以后用户说“继续优化”时默认：

1. 以本原件为唯一基线。
2. 先查当前正式代码，不让用户重复解释项目。
3. 找到根因再改，不做表面修补。
4. 同一类问题一次检查完整链路。
5. 不只改 UI，还要核后端真实逻辑。
6. 不只检查开仓前，还要检查成交后、Worker 重启、部署、恢复、对账和异常。
7. 新资金风险优先按权益比例，不用固定 U。
8. 关键交易修改增加回归测试。
9. 不再用会递归发邮件的 GitHub Actions 临时验证。
10. 普通开发由开发侧完成。
11. 只有必须本人完成的 Gate / Cloudflare 安全动作才让用户操作。
12. 最后优先只告诉用户：问题是否解决、现在需要做什么。

---

# 33. 历史关键提交索引

以下是从部署到当前实盘成型的主要节点：

### Cloudflare / 部署

- `8c600236be936e61845eae0b5df94ffb705455b0`：Cloudflare 一键部署。
- `8d83aad93318a7defdf780811138463f77660523`：修复重复 D1 binding。
- `27830ab499b1e32f30de5a8aeaab58003805a9ea`：binding 回归测试。
- `2ea5e9e49558343b275203878882fc9b3ce50e58`：部署仓库修复 D1 binding。
- `fcb4cabcb8646e5f7d7f179742cb290f9139876f`：修复静态 CSS/JS 路由。

### 早期固定 15U

- `9f81fa2f39fdeb5e199a0957b2d911adbc929399`：加入 15U TP2 净利润检查。
- `d0eed26fa508ffc589a3b45366e85b6bdca7e049`：阻止低于 15U 的订单。
- `eef712ee5c8c1a6cb029f56289300ad8a3646318`：UI 显示 15U 门槛。

这组规则后来全部被权益比例取代。

### Gate 实盘初版

- `dba684e8d201ea195f4f04b86be4f0fb76c857b6`：TestNet-first Gate 实盘执行。

### 固定 U → 权益比例

- `4b09df393685dd32e76ccd9871250f30d7f5ad90`：TP2 改为权益 1.5%，新凭据只允许 Gate live。
- `429e0fb3fa34f6815dca2cfd165b428f9c8d402b`：全部货币风险迁移到权益比例。
- `ada4296fb15f93e4461c79c8910aa36219e6013c`：完成权益风控策略。
- `563da47b032aa3a5fd3b4c0717fcda26d9a61efd`：统一模拟与 Gate 实盘比例风控。

### 旧信号保护

- `9ad28282b96ff6ed88c34675b5374dd99037b483`：拒绝过期候选。
- `724b2c150bfb4bfc063aa0b926c6c51fc6c4f7b9`：正式加入 2 分钟新鲜度边界。

### 部署安全

- `388c73fb44f6ea2cf9fa8f83a450d4bc8faf1c14`：Cloudflare Version Metadata。
- `7d85d4d1b2e62d04b318b2274a6080290fd169be`：部署 disarm。
- `c0b260daf01809c8de5359e58837486d347dea6a`：新版本部署自动关闭 Gate 新开仓。

### 组合风险

- `380a388099d68098072f11ea9ee87f24d5fc0424`：同方向仓位保护。
- `39266940035f606af5696c1087cd0f2aedfba9f0`：最多 3 仓、同方向最多 2 仓。

### 近期表现

- `26427cf72060778cfd4acb214377df6d26123be0`：近期表现闸门。
- `765fdae6b8e441b6e4e639ab928181972972d4ef`：实盘开仓前强制检查近期表现。
- `1743b1306c6aeb928dcd45fdd674d3462fbfb430`：收紧连续亏损归因。
- `afcc1b6ecbf760a1ea39a634d5259adbbbfc24ad`：完成近期表现与连续亏损保护。

### UI 状态

- `a6a192bf1b298a25ff03b37028d48199ca578f05`：修正顶部行情与实盘状态语义。

### Cloudflare Gate fetch

- `69e625b82bb1cf3872daa9e37d4964c4392db68b`：修复 `Illegal invocation`。
- `a1842dd17c5f1af6cb6ab469197ff3fc8e5b4f2f`：回归测试。

### futures-only API

- `b2e362a17dfe6e0fa6dec733ad029887d164a028`：允许 futures-only 验证。
- `74b7b4c7c3685ab8eb4379c873c350c6975169ad`：纳入正式测试。

### Gate 单向持仓

- `8bf147c85661aae12a5a03e57c011f95d324ceae`：安全切换 helper。
- `84b1e075ef1600583fac9de4397f285d6257bb28`：所有者确认接口。
- `45de8132b54bc5b84d77b4cdc7ebb51d3236444a`：一键切换 UI。
- `640fbe21cc4b5252109712de83727df10f723d02`：纳入正式安全测试。

### Gate 逐仓可用额度

- `3cb83561fe90e29f07a741a2095d9ec38f9ff372`：澄清验证时逐仓可用额度。
- `75b5aa49120840b5bcb370ce087b2ff4184ffd1f`：实盘保证金只按 Gate 逐仓 available。
- `d46e1e338a728d22b51391c55e39d547cc23511b`：逐仓额度回归测试。
- `16df8b842678fc976b7e72731f8afe6e9158a16d`：纳入完整测试，本原件代码基线。

---

# 34. 原件基线确认

**哨兵量化原件代码基线：**  
`16df8b842678fc976b7e72731f8afe6e9158a16d`

**永久留档分支：**  
`sentinel-original-baseline-2026-08-25`

**正式仓库：**  
`alicia5574188-del/market-sentinel-free`

**正式地址：**  
`https://market-sentinel-free.alicia5574188.workers.dev/`

以后任何“继续、更新、优化、修改程序”的默认含义都是：

> **从《哨兵量化原件》继续，不重新做，不回旧版本，不丢失已经完成的实盘安全边界。**

---

# 35. 一句话项目定义

**哨兵量化原件 = 一个以 iPhone 为主要控制端、Cloudflare 24 小时后台运行、使用多维市场证据确认交易、保留模拟验证、可连接 Gate USDT 永续小额实盘、API 服务端加密保存、自动开仓由总开关控制，并具有权益比例风控、旧信号保护、部署保护、组合风险、连续亏损冷却和交易所端保护机制的量化交易程序基线。**

---

**原件定版：2026-08-25**  
**以后所有版本从此继续。**
