# Status

Done on production at commit `7fc1c425a8ffffc66938937b45083feb7e9c3d1e`. GitHub Actions run #538 completed verify, deploy, and monitor successfully, including advancing PAPER authority, schema/credential-presence checks, homepage/history smoke tests, and the actual Gate futures candle proof.

The authoritative universe is permanently BTC, ETH, and SOL; checkpoint recovery removes ZEC and every prior rotating symbol. Each market has an independent freshness decision, so one recovering feed no longer labels every coin incomplete.

Follow-up deployed at commit `2bd3200ddcc5a604656f06e2d9b3632d3700398d`; GitHub Actions run #539 completed successfully. The runtime API was healthy and all three markets reported both primary and ancillary evidence fresh. The false `数据恢复中` display was traced to the browser treating a single request lasting over five seconds as a total market failure. The UI now allows a 30-second request and retains a verified runtime snapshot for 90 seconds across transient transport delay; backend per-market freshness remains authoritative and still blocks stale-price entry. Production browser verification now shows `后台运行中`, BTC and SOL `继续观察`, ETH `等待进场`, live prices, and all three 72-candle charts.

Second follow-up deployed at commit `79f9bdaff22ace37622fad8d78f8c8ca228deb10`; GitHub Actions run #540 completed successfully. Transport freshness is now presentation-only: a slow phone refresh shows a page-delay notice while preserving the last authoritative per-market state, and only actual backend `stale`, `fresh`, or `ancillaryFresh` failures produce `数据恢复中`. Order cards now identify `notional` as PAPER futures contract notional and show a display leverage tier plus estimated margin, with an explicit note that trigger-time price/equity/risk are recalculated and no Gate order is submitted. Production verification showed backend running, BTC observing, ETH and SOL waiting to trigger, all three candle charts, and order-card values including 10×/391.49 U and 20×/534.62 U.

Commit `76070bd8fe74f2117d914670a2ffa83fe0d9287b` deployed successfully in run #541 in 2m38s, down from #540's 3m12s. It fixes the zero-fill plan lifecycle found from production evidence: the former 90-second plan expired before distant predictive entries could be reached, neutral or slightly drifting two-second decisions cancelled frozen intent, and sub-friction targets appeared in Orders only to fail trigger-time economics. Plans now freeze for 15 minutes, survive neutral/same-side drift, and pass fee/slippage economics before display. A neutral-tick crossing is directly tested to open PAPER. The bottom navigation is fixed to the safe-area bottom, tab selection scrolls to the page top, and narrow order headers stack without collision. Production showed three executable plans with positive displayed R:R and fixed navigation. A final display-only candidate makes all card levels use the frozen plan rather than mixing them with a newer recalculation, and serializes scheduled monitoring with deploys so their health probe cannot collide with a Worker restart. Local typecheck, lint, 53 direct tests, 12 architecture/migration tests, build, and diff check pass.

Each card displays actual completed Gate USDT futures candles with working 1m, 15m, and 1h switches. MarketStream mirrors its own nine strategy candle sets to the existing D1 settings row at most once per five minutes, keeping chart reads independent from the two-second trading loop. The chart contains candles, liquidity regions, and strategy/position levels; adjacent Chinese text states direction and whether the system is waiting for real-time price, forming a plan, or holding. A failed period switch can never relabel an older period.

Production visual acceptance observed `后台运行中`, exactly three market cards, 216 rendered candles, no ZEC, and distinct BTC 1m versus 1h OHLC values. The release remains physically PAPER-only; the public LIVE control is visible but security-locked and no Gate mutation endpoint exists.
